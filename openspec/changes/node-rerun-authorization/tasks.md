# tasks — node-rerun-authorization

## 0. 前置裁决（阻塞，未完成前其余任务不得开始）

- [ ] 0.1 架构 §3 终局能力 4 的「终态复活」措辞修订取得用户裁决：接受修订 → 走主方案；不接受 → 改走 design.md「备选方案」或撤回本 change。**架构文档由用户修订，Agent 只提案**

## 1. fold 层与授权证据

- [ ] 1.1 `src/domain.ts`：fold 区分携带有效授权证据的 `succeeded → ready` 与不携带者——前者允许，后者**继续拒绝**。授权证据 MUST 是事件内持久内容，不得是调用时内存标志。先失败后通过。验证：单测
- [ ] 1.2 **重放一致性**：把含重跑的事件流重放两次，断言两次折叠结果完全一致（保证投影仍是纯 fold、冷恢复可复现）。验证：单测
- [ ] 1.3 DAG 投影升 `stateVersion`；重跑与过期标记经既有 `pactflow/node-updated` 的 payload 可选字段表达，**不新增事件类型、不升生产者版本**。验证：单测断言旧读端解析不失败、既有会话保持可写

## 2. 重跑入口与失败关闭

- [ ] 2.1 新增重跑 `@Remote()`（与 `retryNode` 分离）：revision CAS、保留全部历史 Run、新 attempt = 最大值 + 1。先失败后通过——先用测试复现当前 `cannot retry from state succeeded` 原文。验证：单测
- [ ] 2.2 **五类失败关闭逐条实现并各配一个用例**：活跃 Run / 已合并入默认分支 / `archived` / `paused` / 需求处于 `closing` 或 `deployed`。每条错误 MUST 指名具体原因，不得笼统。验证：单测（5 条独立用例，先失败后通过）
- [ ] 2.3 Agent 面不得自行发起：Agent 直接请求重跑被拒绝；重跑只能经既有原生方案确认取得人的明确选择后由宿主执行。验证：单测（对抗性守卫）
- [ ] 2.4 **挂机排除**：`src/host/autopilot-driver.ts` 自动选择逻辑识别「方案含重跑」→ 不自动选中、暂停并持久报告交人。**先失败后通过**——先证明当前挂机会自动选中含重跑的方案。验证：`autopilot.spec.ts` 新增用例
- [ ] 2.5 **判定不可得即拒绝**：「是否已合并入默认分支」的祖先链查询失败/超时 → 拒绝并指名「无法判定」。先失败后通过——先证明当前会失败开放。验证：单测

## 3. 代码输入过期检测接入真实路径

- [ ] 3.1 重跑授权路径调用 `staleCodeInputs`，以**重新解析的依赖当前提交**比对上次运行记录；过期项含依赖标识/分支/上次提交/当前提交四项。验证：单测断言检测被真实调用（此前该函数零生产调用点）
- [ ] 3.2 派发使用重新解析的当前基线，**不使用历史基线**。验证：单测
- [ ] 3.3 依赖当前提交不可解析时显式报告，MUST NOT 呈现为「未过期」。验证：单测
- [ ] 3.4 检测不自动触发重跑：发现过期项后节点状态不变、无派发。验证：单测

## 4. 下游标记（不级联）

- [ ] 4.1 重跑成功产生新提交后，以本节点为代码输入的下游已成功节点被标记为输入过期；标记指名上游依赖与提交变化。验证：单测
- [ ] 4.2 **不级联对抗守卫**：断言下游节点状态仍为 `succeeded`、未被派发、无新 Run 产生。验证：单测
- [ ] 4.3 过期标记在 DAG 与需求视图可见。验证：client 单测 + overlay e2e
- [ ] 4.4 **运行中的下游**：上游重跑时正以旧输入 `running` 的下游，在其结果落账时立即带过期标记。验证：单测
- [ ] 4.5 下游过期不触发挂机自动重跑下游。验证：`autopilot.spec.ts` 新增用例

## 5. 审批失效与预算

- [ ] 5.1 重跑后收口被既有交付对象漂移检测拒绝并要求重新确认；重跑前的验证批准不计为对当前交付对象的有效批准。验证：单测（复用既有 `subject-drift` 机制，断言确实命中而非另写一套）
- [ ] 5.2 重跑计入既有 attempt 上限，不另起计数；超限进入既有持久 `paused` 并落账、派发指名拒绝。验证：单测

## 6. 界面

- [ ] 6.1 重跑授权入口在决策点展示过期依赖明细（四项字段）与不可解析项；授权是显式动作，非默认勾选。验证：client 单测 + overlay e2e

## 7. 终验

- [ ] 7.1 真实端到端：真实依赖链上「A 重跑产生新提交 → B 被标记过期 → 授权重跑 B → B 使用 A 的新提交」全链；断言历史 Run 完整保留、attempt 递增、审批失效生效。**无 mock 冒充**
- [ ] 7.2 真实挂机对照：挂机授权下模型推荐含重跑的方案，断言挂机暂停交人而非自动选中
- [ ] 7.3 **跨 change 复核**：若 `gitea-review-gate-closure` 已实施，复核「已合并入默认分支 → 拒绝重跑」在其外部合并标记下仍成立；若 `need-comment-threads` / `need-attachments` 已先实施（生产者已升版），本 change「老会话可写」断言在当前版本号上重跑。验证：对应单测全绿
- [ ] 7.4 回归：`pnpm run check` 全绿、`openspec validate --all --strict` 全绿
- [ ] 7.5 文档同步：`docs/CURRENT_STATUS-当前状态.md` 的 `code-input-staleness` 由 **partial（触发路径不可达）** 改为 passed；`docs/remaining-work-priorities-待办优先级.md` 的 F03 条目关闭；实施状态记录裁决推翻与验证证据
