# A03 / A12 完整形态 · 决策提案（供裁决，未改实现）

**状态**：草案，等用户裁决。本文件**只提案**，不改变任何行为；任何部分被采纳后才走对应的 OpenSpec change。
**来源**：GPT6 评审 A03（`docs/gpt 评审/PactFlow-全面评审-20260910.md:205`）、A12（`docs/gpt 评审/review-report.md:258`）。

---

## 1. A03：命令被批准 ≠ 测试实现不可被改弱

### 上游原意
授权校验精确到 command/args/timeout/order 是**必要**条件，但可执行内容仍来自任务仓库——登记 `npm test` 并不能阻止 Worker 改 `package.json` 的 test 脚本或削弱断言。不建议禁止改测试（测试本就随功能变），而是要求：
1. 两类证据：**宿主控制的稳定验收基线/外部断言** + 任务新增/修改的项目测试；
2. 测试基础设施/构建脚本/安全配置的改动**进入单独审查**；
3. **空验证**在 UI 与 release 中明确标识「没有执行自动验证」，不得统一显示 verified；
4. 按任务类型配置**最小必需验证策略**，且不能由模型临时降低。

### 已交付的部分
- ②「验证基础设施改动可见」：`validation-integrity.ts` 记录 `validationSensitiveChanges`（已入 `gitResult`），`validation-integrity.spec.ts` 覆盖。
- ③的一半「零验证可识别」：**已交付**（change `harden-wire-validation-count`）——复核合同后确认「Host SHALL 能报告执行数量、零可识别」属**既有合同未接线**（非新目标），故已把 `validationExecutedCount` 接进只读移交摘要（`artifacts[].validationsExecuted`）。**前端 UI 的视觉标记仍未做**（属本提案 A03-a 的 UI 部分）。

### 未交付（需裁决的最小切片）

| 切片 | 内容 | 影响面 | 风险 | 建议 |
| --- | --- | --- | --- | --- |
| A03-a | 把「零验证」变成**一等只读信号**：在 `snapshot`/交付视图暴露 `validationExecuted: boolean`（或 `validationCount`），并在保留现有 UI 文案不回归的前提下标注「无自动验证」 | 只读字段 + 客户端小改；不改领域状态机 | 低 | **建议做**（小、可测） |
| A03-b | **最小必需验证策略**：按任务类型声明“至少要有哪些验证”，派发/收口时校验 | 需新配置项 + 策略语义 | 中（新目标语义） | **需你定目标**：是否引入「按类型最小验证」概念？ |
| A03-c | **宿主侧稳定验收基线**：Host 另存一份独立于任务仓的断言并在候选提交上运行 | 需新的「基线资产」所有权与存储 | **高**（新能力、安全边界） | **建议暂缓**，除非明确需要 |
| A03-d | 「测试基础设施改动进入单独审查」（把 `validationSensitiveChanges` 接入评审门禁/UI） | 依赖 A03-a 与前端 | 中 | 可与 A03-a 合并考虑 |

### 需要你回答
- Q1：**A03-a 是否做？**（我认为应当做：它把已有的「零验证」数据变成用户可见的诚实标识，且顺带消除一处「导出未接线」。）
- Q2：**A03-b/c 是否新增目标？**（b 是策略语义、c 是宿主持有独立断言；二者都超出「加固既有行为」，属**新能力**，按 AGENTS.md 需你先定目标。）

---

## 2. A12：有明确的退出与可移交路径

### 上游原意
- 关闭浮层 ≠ 取消持久任务；「停止观察 / 停止任务 / 保留成果 / 清理资源」必须可区分；
- **卸载前**应检查活跃任务与待清理责任，决定阻断卸载或显式受控 drain，不能只验 health 404；
- 即使插件不再启动，也应能导出只读项目摘要、**精确 Git 产物引用**与未完成责任；旧日志的 reader 版本与安装包可追溯。

### 已交付的部分
- 只读移交：`project-handover`（`projectHandoverSummary`）已导出项目/阶段/精确 Git 产物/未完成责任，`project-handover.spec.ts` 覆盖。
- 本地失败保留与清理责任账本：`local-failure-retention`、`cleanup` 账本。

### 未交付（需裁决的最小切片）

| 切片 | 内容 | 影响面 | 风险 | 建议 |
| --- | --- | --- | --- | --- |
| A12-a | **卸载前 drain 检查**：卸载流程先读活跃 Run 与待清理责任，非空则**阻断卸载并给出清单**（或需显式 `--force-drain`） | 卸载路径 + 只读查询 | 低-中（阻断式，需谨慎文案） | **建议做**（与 §9.2 卸载说明互补；实现为**只读检查 + 明确阻断**，不做自动清理） |
| A12-b | **可移交的安装包/reader 版本可追溯**：在移交摘要中附「compat reader 版本 + 包版本」 | 只读字段 | 低 | 可与 A12-a 合并 |
| A12-c | **前端移交入口**：在 UI 提供「导出移交摘要」按钮 | 客户端 UI，需浏览器套件 | 中 | **需你定**：是否本轮做 UI |

### 需要你回答
- Q3：**A12-a 是否做？**（阻断式 drain 检查。我倾向做：它把「卸载不能只验 health」变成真实约束，且可测。）
- Q4：**A12-c 前端入口是否本轮做？**（需 UI 改动与浏览器验证；若你只要能力、暂不要 UI，可只做 a+b。）

---

## 3. 我的整体建议（若要最小风险推进）

1. **做 A03-a**（零验证一等信号，小、可测，顺带消除未接线导出）；
2. **做 A12-a + A12-b**（只读 drain 检查 + 版本可追溯，明确阻断、不做自动清理）；
3. **A03-b/c、A12-c 先定目标再排期**——它们引入新的策略/基线/UI 语义，属新目标；
4. 每项单独走 OpenSpec change（propose → apply → verify → archive），与本批一致。

**不做也不影响当前正确性**：A03-b/c 与 A12-c 缺省下，现有行为是安全的（拒绝空验证呈现为 verified 的**数据**已在；drain 缺省为「照常卸载」但 §9.2 已提示需人工确认）。

## 4. 分类已用合同核对（2026-09-11，避免再次误分类）

本会话曾把 `validationExecutedCount` 误列为「待裁决的新能力」，实为**既有合同未接线**（已修正并接线）。为避免同类误判，本提案的每项都**逐条对照 openspec 合同**核实：

- **A03-a**（零验证可识别）：合同 `validation-integrity-signals` 明确要求「Host SHALL 能报告执行数量…零必须可识别」→ **既有合同** → 已接线（`artifacts[].validationsExecuted`）；剩余 UI 视觉标记才属新目标。
- **A03-d**（测试基础设施改动进入审查）：合同 `validation-integrity-signals` 要求「在运行结果中记录这些文件路径」→ **已满足**：`validationSensitiveChanges` 经 `syncResult` 落入 `PactFlowGitResult`（`types.ts` 的 `PactFlowGitResult.validationSensitiveChanges`），经 `snapshot()` 可读。故「记录」不缺；缺的是**评审门禁/UI 呈现**（新目标）。
- **A12-a**（卸载前 drain 检查）：`grep` 全部 `openspec/specs/*/spec.md` **无任何**「drain / 卸载前」要求 → **确属新能力**，须你定目标。
- **A12-b/c**：同上，无对应合同 → 新能力。

即：需你裁决的只有**没有合同依据**的部分；有合同依据的已在（或已在本会话）交付。
