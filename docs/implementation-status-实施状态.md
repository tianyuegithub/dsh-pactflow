# DSH 零脉实施状态

## 2026-09-06 持续修复（执行中）

- 当前源码基点：加固分支 `77f7108`；主目录 `main` 仍为 `818a1d9`。
- 复核结论：旧“阶段一至三全部完成”声明撤回。权限、模式、跨进程持久化、恢复、清理和发布证据仍存在明确缺口。
- 本批改动（尚未提交）：公开阶段接口禁止直接进入已部署，正常 Git 收口通过内部方法推进；审批展示待记录决定、修订和证据正文；沿用既有证据大小和疑似凭证校验，在请求审批前与宿主落账前共同执行。该校验不声称能够识别所有形式的秘密。
- 宿主验证命令增量：非空原始命令在绑定前拒绝；执行器要求独立传入当前登记配置解析的授权命令，精确核对命令、参数、超时和顺序。本地、K3s 结果接收、冷恢复、收口和现有清理重试调用均传播授权；旧运行自报配置编号不能授予执行权。空命令配置继续合法。
- 最近验证：原授权回归 8 项中 3 项失败、5 项通过；修复后原失败点及正常收口通过。原始绑定、旧运行和伪造配置编号的 3 项新回归先失败后通过；恢复测试确认旧命令不执行。`pnpm run check` 通过：15 个文件、86 项测试及 13 项包产物检查。随后补充同编号命令/参数/超时漂移与配置版本变更断言，定向 4 项通过；`git diff --check` 通过。
- 命令授权复核补证：独立复核未发现可利用绕过，但要求补登记非空命令的恢复成功及收口授权矩阵。已补齐空配置、登记成功、版本漂移拒绝、旧绑定拒绝，`closing.spec.ts` 与 `k3s-recovery.spec.ts` 共 7 项通过；冷恢复模拟资源已补齐 UID、所有者、哈希、镜像摘要、分支及基线。
- K3s 身份修复：缺字段矩阵原 6 项失败已转绿。结果接收强制完整 UID、三项哈希、分支、基线与固定镜像摘要，不再按生产者版本放行；镜像完整摘要替代子串匹配，额外字符伪造用例先失败后通过。身份不足的持久清理/取消拒绝名称删除并留失败账本，创建过程内的私有补偿路径保留。
- K3s 复核：限定结果身份差异的独立复核通过，3 个文件 34 项测试通过；不覆盖完整子资源所有权及真实容器运行时差异。
- 投影持久化增量：项目资源池及 K3s 资源池、项目配置版本、Agent Profile（智能体配置）、模型连接和临时密钥标记解析丢失已修复；项目/运行投影缓存版本升至 2。三个失败回归已通过，并通过 DSH 实际恢复接口证明版本 1 缓存失效后从事件序号 0 重放。无原始事件的已丢失字段无法凭空恢复。
- 类型校验增量：移除 `as unknown as ZodType`；模式适配器编译期检查结构，显式校验品牌标识字符串，并在解析输出移除可选 `undefined` 属性。事件引用和状态迁移完整校验仍待后续实现。
- 最新验证：`pnpm run check` 通过，16 个文件、114 项测试及 13 项包产物检查；`git diff --check` 通过。
- 工作区读取增量：移除一次性缓存与失败 Promise（异步结果）缓存；每次读取重新加载磁盘，并与同进程写入串行。非法顶层、坏记录和重复工作区编号失败关闭，不跳过坏行；全部校验通过后才替换内存状态。首次失败后文件修复，同一实例能够恢复。无条件 `put()` 已移除，测试初始化统一使用修订比较写入。
- 最近验证：上述 5 项回归先失败后通过；`pnpm run check` 通过，16 个文件、119 项测试及 13 项包产物检查。跨进程写入互斥仍未实现，不能以本批读取修复宣称 CAS（比较并交换）整体完成。
- 跨进程互斥增量：文件目录锁覆盖读取、修订比较与原子替换，同进程队列继续串行。锁内记录进程编号，正常完成或异常返回均释放；等待超过 5 秒明确报错，不依据锁龄抢占。
- 多进程证据：12 个独立 Node 进程同时竞争同一修订，修复前 12 个均返回成功、修复后仅 1 个成功；12 个进程写不同工作区，修复前仅剩 1 条、修复后 12 条全部保留。锁超时不抢锁、不删除所有者记录测试通过。`pnpm run check` 最新通过：16 个文件、122 项测试及 13 项包产物检查。
- 剩余锁生命周期风险：进程被强制终止可能留下目录锁，当前只会超时报错，不自动移除。须确认写入者已停止后处理精确锁目标；尚未交付自动崩溃锁恢复，不能宣称配置存储生命周期完整闭环。
- 容量恢复增量：已配置项目/资源池的既有占用不再受缩容后上限拒绝；统一调度器调用恢复接口，不复用新任务准入。资源池恢复抛错时回滚已登记项目占用，重复释放仍幂等；资源池队列在占用真正降至上限以下才放行。
- 容量证据：超配恢复及恢复异常泄漏两个回归先失败后通过；队列提前放行回归先失败后通过。`pnpm run check` 通过：16 个文件、125 项测试及包产物检查。跨所有会话启动盘点、缺失配置的恢复处置和统一队列唤醒仍需继续验证，不以本批容量原语修复替代完整重启验收。
- 统一队列增量：修复重入保护导致仅唤醒队首、余下请求永久停留的问题；关闭标记拒绝新准入与恢复预留，关闭后的释放不会重新启动调度。两个失败回归已通过，保留取消队首、关闭清队列、幂等释放和 FIFO（先进先出）顺序。
- 最新验证：`pnpm run check` 通过，17 个文件、129 项测试及包产物检查；随后新增 512 项满队列排空与第 513 项拒绝回归，`execution-capacity.spec.ts` 5 项通过。跨会话启动盘点及异常退出锁恢复仍未完成。
- 恢复错误分类：优先解析结构化 HTTP（超文本传输协议）状态码，兼容现有包装消息；408/409/425/429 和 5xx 有界重试，其他 4xx 与身份不匹配停止重试。八项误分类回归先失败后通过，取消等待无后续调用/残余计时器、五次重试耗尽保留原错误均已覆盖。
- 最新验证：`pnpm run check` 通过，18 个文件、142 项测试及包产物检查；随后补充耗尽断言，`recovery-retry.spec.ts` 13 项通过。持续临时故障在一批重试耗尽后的长期监督和结算语义仍未修复，不能以错误分类修复宣称恢复链完整完成。
- 清理监督增量：同一会话/清理编号的并发请求共享执行结果，避免重复删除与重复增加尝试次数；新失败记录及恢复出的未来到期记录建立到期计时器，动作成功或开始重试清除旧计时器。回调重新检查最新状态和到期时间，卸载后不再启动任务。
- 清理证据：并发重复执行及两类到期不重试均先失败后通过。`pnpm run check` 通过，19 个文件、146 项测试及包产物检查；最后补充卸载清计时器及回调状态检查后，构建与清理/收口/K3s 恢复共 13 项测试通过。尚未覆盖未激活会话的全局启动扫描，也未解决交付终态前清理账本持久化窗口、精确清理目标和真实子资源删除验收。
- 收口顺序增量：清理责任事件先于交付记录和已部署事件；本地任务不再产生不存在的 K3s 清理项。存在 K3s 资源但服务不可用时走失败账本，不再直接标记清理成功。
- 收口证据：新增事件顺序/真实目标断言先失败后通过。`pnpm run check` 通过，19 个文件、147 项测试及包产物检查；随后追加账本写入失败注入，收口 5 项通过，证明即使 Git 合并成功，账本写入失败也不写交付记录或已部署状态。真实进程崩溃/存储落盘、精确清理目标及重试不重新准备集成分支仍待完成。
- 精确集成清理增量：清理记录保存首次收口的分支、提交及工作目录，交付投影缓存升至 2。重试直接使用保存目标，不再重新拉取/合并/推送；旧记录缺失精确目标或目标不一致时保留失败责任，不猜测删除目标。
- 删除保护：限制插件工作目录范围，校验真实路径、Git 公共目录、当前分支与提交；分支删除使用预期提交比较，重复清理可成功返回。原四项真实 Git 失败回归已通过；额外符号链接越界和旧目标拒绝路径通过。
- 最新验证：`pnpm run check` 通过，20 个文件、153 项测试及包产物检查；随后扩充目标矩阵，精确 Git 清理和清理重试共 12 项通过。本批独立复核待返回；任务分支/K3s 子资源的精确删除与全局恢复仍未完成。
- 精确清理复核后修正：独立复核发现首次账本写入发生在合并之后的恢复缺口及交付缓存重放测试不足。现已在最终合并前保存全部清理意图，并以 `requiresRelease`（等待交付证明）门禁禁止提前清理。首次账本写入失败恢复后，再次收口能够成功；交付缓存版本 1 丢失精确目标时，从原事件重放恢复完整字段。
- 最新验证：`pnpm run check` 通过，20 个文件、157 项测试及包产物检查。仍需覆盖“外部合并成功后交付记录写入失败”的恢复路径；预合并清理意图未完成时，不应回滚到不识别交付门禁的旧实现。任务分支与 K3s 精确删除、全局恢复及发布验收仍未完成。
- 合并后恢复增量：收口意图保存稳定排序的输入摘要；再次收口须匹配需求修订、Git 绑定和任务集合。已合并的 PR（拉取请求）按精确 head（源分支）提交匹配，确认默认分支包含原集成提交、任务远端 tip（分支顶端提交）未变后复用保存身份，不重新创建已删除的集成远端分支，也不再次合并。
- 最新验证：合并后交付记录写入失败回归先因本地未推送分支报错、修复后可再次收口且仅一次合并；`pnpm run check` 通过，20 个文件、158 项测试及包产物检查。随后新增恢复期间任务分支漂移拒绝断言，收口 7 项通过。真实进程崩溃与存储落盘证明仍待环境验收。
- 集成隔离增量：新分支和工作目录同时包含会话摘要、需求编号与修订号，避免同会话不同需求的目录碰撞及不同会话同名需求的远端分支碰撞。已有尝试可按精确匹配的旧命名身份恢复；复用远端集成时校验本地工作目录分支和提交，不复用漂移目录。
- 最新验证：两类真实 Git 碰撞先失败后通过；`pnpm run check` 通过，21 个文件、161 项测试及包产物检查。随后增加旧命名尝试恢复，集成隔离 3 项通过。Git 任务集合基线校验、任务分支条件删除等仍需继续加固。
- 集成任务集合增量：仅遍历本次基线到集成提交的第一父链，要求每一步为预期任务的双父合并，拒绝额外普通提交、重复/未知任务及错误基线。收口参数强制远端引用与预期提交成对提供，缺失预期提交在创建集成资源前拒绝。
- 最新验证：历史合并误拒绝、额外提交误接受两个真实 Git 回归先失败后通过；`pnpm run check` 通过，22 个文件、164 项测试及包产物检查。随后收紧参数合同，构建与任务集合/收口共 10 项测试通过。任务分支条件删除、K3s 子资源与其余原计划验收继续进行。
- 任务分支清理增量：显式传入历史成功提交，校验远端绑定与默认分支排除；复用本地工作目录归属/提交检查，远端不存在视为已清理，远端存在仅允许预期提交条件删除。删除请求携带引用租约比较，不改写分支历史；本地删除同样使用预期提交条件。
- 最新验证：重复清理、远端漂移、目录外目标和缺失提交证据四项真实 Git 回归先失败后通过；`pnpm run check` 通过，22 个文件、169 项测试及包产物检查。随后注入“检查后、删除前远端变化”，Git 清理 10 项通过，变化后的远端分支和本地工作目录均保留。真实 Gitea 删除、K3s 子资源及其它原计划验收仍待完成。
- K3s 子资源增量：持久清理读取 ConfigMap（配置映射）及短命 Secret（密钥对象），核对原 Job 所有者、运行标签/哈希及 UID，再携带 UID 前置条件删除；子资源校验失败时不删除资源或 Job。已绑定运行的取消复用该路径，创建前补偿仍独立。Job 不存在的恢复路径也登记清理责任并验证子资源结果，不直接返回漏账。
- 验证波动记录：默认文件并发下全量测试出现真实 Git 5 秒超时（首次还存在构建重叠干扰），独立重跑仍有 9 项超时；保持断言和超时不变，文件并发限制为 2 后 172 项通过，测试内部 12 进程 CAS 竞争仍保留。测试脚本已固定该并发上限。
- 最新验证：`pnpm run check` 通过，22 个文件、172 项测试及包产物检查；最后统一已绑定取消路径后，构建及 K3s 清理/结果/恢复 36 项通过。独立子资源复核待返回；显式 Pod 清理、创建前补偿所有权、真实集群与其余原计划验收仍未完成。
- 显式实例清理：按 Job 控制器 UID 筛选 Pod（运行实例），校验运行哈希并携带实例 UID 删除；其他任务实例与 34 个模拟历史无所有者实例均不触碰。身份错误或删除冲突时不继续删除 Job。
- 独立复核补证：配置映射/密钥对象复核未发现具体所有权绕过，要求补足错误哈希、409/500 阻断、删除竞态 404 幂等及缺失任务清理调用轨迹；以上隔离断言已补齐并通过。复核不覆盖后来新增的实例清理，不等同于真实集群验收。
- 最新验证：`pnpm run check` 通过，22 个文件、178 项测试及包产物检查。真实集群资源完全消失的证明、创建前补偿所有权、事件合法性、全局恢复及发布验证等原计划内容仍未完成。
- 需求事件校验增量：实时接口与事件重放共用合法阶段顺序；创建必须从初始修订/待办阶段开始，普通更新不能改变阶段，创建时间不可变，阶段事件不能跳过中间阶段。需求投影缓存升至 2，使旧缓存不能绕过重放校验。
- 最新验证：三个损坏事件回归先失败后通过；`pnpm run check` 通过，23 个文件、181 项测试及包产物检查。随后补初始状态非法与合法普通更新对照，事件/领域共 24 项通过。跨需求/节点/运行引用完整性和其它原计划门禁仍未完成。
- 任务图事件校验：节点所属需求不可变；创建或改变依赖时校验缺失引用、跨需求引用、重复依赖、自循环与间接循环，任务图缓存升至 2。检查采用迭代遍历，不因长依赖链增加调用栈深度。
- 最新验证：缺失/跨需求/自依赖/循环/改归属五项回归先失败后通过；`pnpm run check` 通过，23 个文件、189 项测试及包产物检查。节点对应需求是否存在、运行引用与状态迁移等跨实体校验仍待继续完成。
- 运行事件校验增量：运行与事件节点编号/修订必须匹配，结算采用执行修订加一的节点关系；终态不能通过续租复活。执行提供者、Git 规格、K3s 规格不可在运行中替换，唯独合法绑定事件可补入 Job UID；规格比较不受 JSON 属性顺序影响。运行投影缓存升至 3。
- 最新验证：终态复活、节点修订不一致、执行提供者替换、Git 规格追加四项回归先失败后通过；`pnpm run check` 通过，23 个文件、193 项测试及包产物检查。跨实体存在性、完整阶段门禁及其余原计划验收继续进行。
- 需求存在性增量：任务图内部状态保存从会话事件推导的需求编号索引，拒绝引用未创建需求的节点；任务图缓存升至 3。公开任务图、快照和网页视图保持原结构，不暴露内部索引。
- 最新验证：不存在需求引用回归先失败后通过；`pnpm run check` 通过，23 个文件、194 项测试及包产物检查。随后通过旧缓存重放及公开视图无字段变化对照，投影/事件共 21 项通过。交付引用与完整权限阶段门禁等原计划事项仍未完成。
- 清理事件身份增量：同一清理编号的资源身份不可在重试中改变，仅状态、尝试次数、错误和下一重试时间可更新；目标、所属需求/运行、精确工作目录、输入摘要及交付门禁均冻结，交付缓存升至 3。
- 最新验证：目标、需求、门禁和精确目录四项篡改回归先失败后通过；`pnpm run check` 通过，23 个文件、199 项测试及包产物检查。完整权限阶段门禁、全局恢复及其它原计划验收仍未完成。
- 阶段内容分离：阶段事件不得夹带标题或需求描述修改，正常内容更新仍使用独立更新事件。新增描述替换回归先失败后通过；`pnpm run check` 通过，23 个文件、200 项测试及包产物检查。
- 待用户决策：既有合同规定领取仅检查节点就绪；是否将全部任务图节点限定为计划获批后的实施任务（执行/代码评审/验证阶段），还是允许前期调研节点，尚待确认。此门禁暂不修改，其余冻结范围可继续实施。
- 交付引用校验：内部保存需求存在性及运行归属索引，拒绝评审/文档/交付引用不存在需求，拒绝清理引用不存在运行或跨需求运行。交付缓存升至 4；公开快照与网页视图不包含内部索引。
- 最新验证：六项缺失/跨归属引用回归先失败后通过；`pnpm run check` 通过，23 个文件、206 项测试及包产物检查。随后加强索引自有属性检查与公开视图对照，构建及投影/事件 32 项通过。阶段决策、全局恢复、网页和发布验收继续待完成。
- 持续恢复增量：临时错误耗尽单批重试后，保留非终态运行和已占用容量，按有上限的退避继续监督，不据此结算失败。确定性验证/授权错误仍终止恢复。宿主关闭撤销恢复计时器并停止观察，不因停止观察删除可重连的 Job；普通运行取消仍保留删除语义。
- 最新验证：连续 5 次 503 后恢复回归先失败后通过，验证只预留一次容量、终态后释放一次；`pnpm run check` 通过，23 个文件、207 项测试及包产物检查。随后补监督关闭和观察取消对照，K3s 恢复/执行 30 项通过。全局启动盘点、配置缺失恢复和真实跨重启验收仍未完成。
- 观察取消竞态：取消在 Job 读取返回期间发生时，轮询等待不再创建已无法收到取消通知的新计时器。该回归先失败后通过；`pnpm run check` 通过，23 个文件、210 项测试及包产物检查。未激活会话恢复需继续核对 DSH 的官方会话生命周期入口，不绕过其持久化/发布机制。
- 全局恢复接口调查：本地 DSH 源码的 `sessionController.resolveAgent()` 提供并发去重恢复，经官方 `agents.resume()` 完成持久加载、预设组合和注册发布；`ReactLoopAgent` 构造为空闲态，PactFlow 预设启动监听器未直接驱动模型。可作为后续恢复入口的候选，不自行附着无 Agent 的会话或直接改写日志。此结论是源码证据，尚未证明受支持发行版提供同一能力，也未完成启动扫描、先盘点容量再准入的运行验证。
- 恢复准入屏障：会话恢复先登记全部既有运行容量，再启动观察；已登记容量转交运行生命周期，不重复预留。嵌套盘点共同暂停准入，盘点失败按会话保留错误，未全部修复前拒绝新请求；空运行会话可清除其旧盘点错误。
- 最新验证：`pnpm run check` 通过，23 个文件、212 项测试及包产物检查；随后补会话内两个运行的登记/观察顺序断言，构建及调度/恢复 16 项通过。全局未激活会话扫描尚未接入，该屏障不能替代完整启动恢复验收。
- 全局扫描接入：会话查询与控制器可用时，读取原生会话/投影，筛选仍有非终态运行或未完成清理的 PactFlow 根会话，通过官方控制器恢复，不发送模型输入；整个扫描和容量盘点持有准入屏障。失败保留准入错误并有界退避重试，卸载终止监督；运行观察和清理后台继续，不等待所有任务终态才开放已盘点容量。
- 最新验证：冷根会话筛选、自动服务挂接、扫描失败拒绝准入及成功重扫清错均有隔离验证；`pnpm run check` 通过，24 个文件、215 项测试及包产物检查。随后启动扫描/调度 10 项通过。控制器恢复边界使用测试替身，真实官方发行版、模型无调用及跨宿主启动仍需实际验收；阶段决策仍待确认。
- 扫描取消隔离：已取消扫描在异步列表/会话读取和容量盘点返回后不再清除新失败状态；取消时不将错误覆盖为成功。迟到空列表回归先失败后通过；`pnpm run check` 通过，24 个文件、217 项测试及包产物检查。
- 发布门禁增量：`check:release` 改由串行脚本执行本地检查、网页测试及安装验证；网页阶段输出结构化报告，校验所有发现的测试文件、非空断言、通过计数及零失败/零跳过/零待办，未满足时不进入安装验证。普通开发 `test:web` 行为不变。
- 最新验证：报告有效、缺失、失败、空测试、缺套件、跳过及待办等 8 项门禁测试通过；`pnpm run check` 通过，25 个文件、225 项测试及包产物检查，两个发布脚本语法检查通过。本轮未运行真实 `check:release`，不宣称网页或安装验收通过。
- 通用任务文案：移除固定业务节点编号到文案的映射；卡片取实际任务标题，保留英文空格和中英混合内容，仅按既有展示长度截断，不再生成无依据的业务说明。状态文案与布局算法不变。
- 最新验证：固定编号覆盖标题及英文内容丢失两个回归先失败后通过；`pnpm run check` 通过，25 个文件、226 项测试及包产物检查。浏览器展示和完整网页测试仍未验收。
- 网页烟测修复：原测试种子将需求直接创建在规划阶段修订 5，被新的合法事件校验拒绝；已改为完整 0.1 历史阶段/评审事件序列，保留旧事件只读验证，并按真实节点标题断言。新增页面来源/标题、框架错误遮罩、浏览器错误日志断言及可选仓外截图。
- 网页实测：隔离本地 DSH 开发构建、Playwright Chromium、1680×1000；零脉入口→原生面板→关闭→设置→插件配置两项通过，8 项真实集成测试明确禁用而跳过。严格发布报告校验确实拒绝该报告，不能据此宣布发布通过；截图保存在本次仓外临时证据目录。最初使用非法快照模式 off 导致初始化失败，已按框架合同改用 replay（回放），未放宽生产校验。
- 官方基线调查：2026-09-06 查询官方 npm registry（软件包仓库），`@deepseek-ai/dsh` 默认分发标签为 `0.1.2-rc.1`；已在独立临时目录安装该确切版本并运行 `--version` 成功，未修改 DSH 源码。临时入口为 `/tmp/pactflow-official-dsh-G58SQD/node_modules/@deepseek-ai/dsh/lib/bin.js`，使用前需重新确认存在。
- 兼容风险：官方 GitHub 已发布 `dsh-v0.1.3-alpha.1`，发布说明明确会话持久化 API 与日志 v2 的破坏性变化（https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.3-alpha.1）。当前插件开发依赖仍指向本地 `0.1.2-alpha.1` 源码分支；默认安装验证脚本也仍使用该分支，不能充当官方兼容证明。官方 CLI 已安装不代表插件安装/启动通过；临时安装报告了 React peer（对等依赖）版本告警，影响尚未验证。
- 官方安装实测：当前源码包在隔离官方 `0.1.2-rc.1` 的 web 配置档安装成功，但启动先失败于 `settingsNamespace` 导出缺失。已按官方 Settings（设置）接口改用固定合法命名空间，不依赖该运行时辅助函数；本地 `pnpm run check` 227 项通过。
- 官方发布阻断：重新打包并安装到第二个全新配置档后，确认 `sessions.externalEventProducers` 不存在，无法注册插件持久事件。已增加明确的不兼容错误，而非跳过注册或使用私有补丁；README 记录这一真实支持边界。当前不能发布或声称官方启动通过。
- 官方卸载对照：已从第二个隔离配置档卸载测试包，随后官方 Web 正常启动，`/api/pactflow/health` 返回 404，确认插件入口移除。卸载只影响本次临时配置档，可由本地安装包恢复；用户配置档、DSH 源码及历史集群资源未修改。
- 安装验证边界修正：默认 `verify:profile` 要求显式 `DSH_CLI_ENTRY`，禁用源码回退；`verify:profile:dev` 才允许开发源码，并标注非发布证据。发布脚本预检同一运行时选择合同；路径参数本身不证明官方来源，来源仍须独立核验。修复启动失败后等待已退出子进程的挂起。
- 最新验证：运行时选择 3 项测试及 `pnpm run check` 通过，共 26 个文件、230 项测试；脚本语法检查通过。缺少入口实际调用立即失败；使用隔离官方 `0.1.2-rc.1` 实际运行验证脚本，因所需外部事件能力缺失正常失败退出，未伪装为安装验收成功。
- 事件解析一致性：投影现在使用 Schema（校验模式）解析后的载荷，不再只校验后读取原始对象；未声明字段不会进入实时投影，原始日志不被修改。事件名与载荷模式有编译期映射约束，相关投影缓存版本递增以重新折叠旧缓存。
- 最新验证：实时投影保留额外字段、而缓存视图剥离字段的不一致回归先失败后通过；`pnpm run check` 通过，26 个文件、231 项测试及包产物检查。官方兼容阻断、待确认阶段规则和其余未完成验收不变。
- 工作区继承增量：仓库检查、本地 Git 派发与收口在无显式会话绑定时继承工作区配置，与现有 K3s 路径一致；不回写会话覆盖，显式绑定保持优先。工作区场景先在仓库检查处复现“没有 Gitea 绑定”，修复后完成本地任务、受保护合并与清理。
- 最新验证：收口 9 项回归通过，包含纯工作区及冲突工作区/显式会话优先对照；`pnpm run check` 通过，26 个文件、233 项测试及 13 项包产物检查，`git diff --check` 通过。测试使用真实临时 Git 仓库、隔离的子代理替身与 Gitea HTTP 测试服务，不替代真实环境验收。排队后配置复核等剩余缺口继续保留。
- 排队后准入增量：取得槽位后重新验证登记命令、工作区完整配置、会话修订和选定执行资源快照；最后同步检查取消信号后才认领。漂移或取消记录队列取消事件，外层始终释放槽位，不创建工作目录或 Job。资源快照包含选定池、集群执行配置、选定模板和模型引用，不包含原始凭证。
- 最新验证：工作区/会话/执行配置漂移及槽位交接取消四项回归先失败后通过；正常准入对照与池/模板/模型/集群快照变更对照通过。`pnpm run check` 通过，27 个文件、243 项测试及 13 项包产物检查；`git diff --check` 通过。新准入测试使用真实宿主事件/投影/队列，在 Git 与 Kubernetes 执行边界使用隔离替身，不代表真实集群验收。
- 远端创建恢复调查：新增 `workspace-remote-recovery.spec.ts`，在请求结果不明与创建返回后推送失败两条路径检查外部操作前已持久化责任。两项均按预期暴露当前缺陷：创建调用之前配置为空，失败后没有创建意图；真实临时 Git 证明推送失败仍留下 origin（默认远端）。测试在 Gitea 创建边界使用替身，不访问真实服务。
- 当前验证状态：上述 2 项新增回归尚未修复，因此当前完整检查不能声称通过；最近一次通过的 243 项检查早于这两项测试。下一安全动作是实现精确目标的持久化创建意图，并保证验证配置、Worker 策略、仓库采用和迁移等配置写入不会覆盖未完成责任，再补恢复/并发测试。不得把同名仓库或模糊 API 结果认定为本次创建成功，不自动删除远端仓库。
- 远端创建意图增量：外部创建前 CAS 写入操作编号、提供者快照、目标和精确提交；收到创建响应后先持久化地址，再推送。存储拒绝丢弃/改向已记录责任，配置保存、仓库采用和迁移保留操作。已创建状态允许按同一目标接续推送；只允许创建不存在的默认分支，已有同一提交视为完成，其他提交拒绝覆盖。完成后保留审计记录。
- 最新验证：前述 2 项失败回归修复后通过，扩展断言覆盖责任不可丢弃、配置保存保留责任、推送失败后恢复、完成后重复调用以及结果不明禁止重复创建。`pnpm run check` 通过：28 个文件、245 项测试与 13 项包产物检查；`git diff --check` 通过。使用真实临时 Git 和隔离 Gitea 创建替身，未运行真实 API 创建。
- 尚未完成：结果不明的精确仓库身份对账、界面恢复入口、崩溃/并发完整矩阵和真实 Gitea 验收。当前保留责任并失败关闭，不声称自动恢复全部失败。存在创建操作记录时，不应回滚到会覆盖该字段的旧配置写入实现；先保全配置并完成对账。
- 远端恢复矩阵补证：增加同宿主并发创建、本地提交漂移、origin 地址漂移、目标分支已有其他提交、推送成功后完成记录写入失败、默认分支不同于本地分支六个场景。验证仅创建一次、漂移不覆盖数据、完成写入失败可复用已推送提交、精确提交推到请求的默认分支；未修改生产源码或放宽断言。
- 最新验证：`pnpm run check` 通过，28 个文件、251 项测试及 13 项包产物检查；`git diff --check` 通过。上述并发证明只覆盖同宿主请求，不能替代跨进程崩溃、API 响应丢失后的身份对账或真实 Gitea 验收。
- 创建可见性校验：创建响应的 private（私有可见性）必须与用户请求精确一致，而非仅检查布尔类型。公开/私有两个方向的不一致回归先失败后通过，正常匹配仍通过。`pnpm run check` 通过：28 个文件、253 项测试及 13 项包产物检查；`git diff --check` 通过。采用本地 HTTP 测试服务，未创建真实仓库。
- 待用户决策：创建结果不明时，是否采用“只读展示候选与核验结果，人工确认精确仓库编号后恢复推送”。Gitea 描述字段可修改，操作编号只能作为关联线索；尚未获得确认，不实施自动认领或扩大恢复授权。其余已冻结事项继续推进。
- 锁崩溃恢复增量：抽取本机配置锁模块，完整所有者文件随非空目录原子发布；确认本机进程不存在时只回收该唯一所有者文件，释放不递归删除目录。旧格式、未知主机和空未知目录继续失败关闭。新增真实持锁子进程被 SIGKILL（强制终止）后的读取恢复，及空未知目录不可抢占两项回归，均先失败后通过。
- 最新验证：`pnpm run check` 通过，28 个文件、255 项测试及 13 项包产物检查；现有 12 个独立进程的同修订竞争、不同工作区并行写入、旧锁保护继续通过，`git diff --check` 通过。当前证明限于本机文件系统；多恢复者同刻回收、不同操作系统及发布前崩溃临时目录治理尚需补验。
- 锁升级边界：必须先停止旧版宿主写入，再升级/回滚锁协议，不能混用新旧写入器。进程编号复用时可能保守阻断；旧格式残留不自动清除。当前仅修改源码，未升级用户运行服务。
- 锁释放复核修正：先删除所有者文件再删除目录存在释放中崩溃留下空锁的窗口，现改为先原子移走完整目录再清理。回收者先原子改名旧所有者文件，以其自身进程身份取得唯一所有权，回收过程中退出后仍可继续判定；不允许竞争回收者操作新所有者文件。
- 最新验证：真实持锁进程退出后 12 个独立写入进程同时竞争，全部 12 行保留；工作区专项 18 项通过。`pnpm run check` 通过，28 个文件、256 项测试及 13 项包产物检查；`git diff --check` 通过。释放每个指令边界的强制退出注入、已移走临时目录残留治理及跨平台仍未完成，当前未升级运行服务。
- 临时锁治理增量：加锁前仅扫描当前配置文件精确命名的暂存/已移走目录；非空残留核验本机死进程身份后回收，活动暂存目录保留。空的已移走目录只使用非递归目录删除；空暂存、未知文件和符号链接不清理。
- 最新验证：死进程暂存和已移走残留两项回归先失败后通过；补活动暂存保留、未知数据及符号链接目标保留、空已移走目录清理对照。`pnpm run check` 通过：28 个文件、260 项测试及 13 项包产物检查；`git diff --check` 通过。测试只删除本次临时夹具中的已确认锁产物，未清理用户运行目录。每个文件系统指令边界的故障注入与跨平台证明仍未完成。
- 嵌套配置校验增量：共享模式增加工作区 Git 绑定和项目执行策略结构校验；存储拒绝不完整嵌套对象、重复执行配置编号及不一致的并发总量。验证配置引用必须能在目录中找到，目录缺失不再绕过检查；读取失败保留原文件。
- 最新验证：不完整 Git/执行策略、缺失验证配置引用、错误并发总量四项回归先失败后通过。`pnpm run check` 通过：28 个文件、264 项测试及 13 项包产物检查；`git diff --check` 通过。此证据不等同于全部 Settings/Remote/投影已共享完整模式，外部资源引用及所有兼容迁移仍需后续验收。
- 验证配置模式统一：宿主保存与存储读取共享命令、参数、超时、名称长度、编号、修订和目录数量模式；删除两处重复规则，宿主保留规范化及过期修订检查。此前读取侧允许超过保存入口名称上限的配置，新增回归先失败后通过。
- 最新验证：`pnpm run check` 通过，28 个文件、265 项测试及 13 项包产物检查；原有 shell（命令解释器）包装拒绝、原始命令拒绝、授权配置执行与修订漂移测试继续通过，`git diff --check` 通过。模式统一仅覆盖已改动配置，不声称全部 Settings/Remote/投影合同已统一。
- 工作区模式归一：配置总结构和远端创建记录并入共享模式模块，存储使用同一模式校验，去掉基于 Partial（部分类型）断言的手工形状判断。旧命令数据保留结构化只读兼容，但空对象等无效数组元素不再通过读取；模式解析本身不授予命令执行权。
- 最新验证：无效旧命令元素回归先失败后通过；`pnpm run check` 通过：28 个文件、266 项测试及 13 项包产物检查，旧命令拒绝执行与创建恢复测试继续通过；`git diff --check` 通过。全量设置/远程接口/投影共享模式及真实环境验收尚未完成。
- 官方基线复查（2026-09-06）：显式 npm 官方源查询仍返回 latest/next 为 `0.1.2-rc.1`、alpha 为 `0.1.2-alpha.5`；`@deepseek-ai/dsh@0.1.3-alpha.1` 查询返回 E404（版本不存在）。GitHub 的 `dsh-v0.1.3-alpha.1` 标签存在，但该标签 `packages/core/session/src/index.ts` 的 SessionStore（会话存储）未提供当前依赖的 `externalEventProducers` 成员；这仅为源接口核查，不是该发行包的安装测试。来源：https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.3-alpha.1 与同标签原始源码。
- 本轮未更改依赖或用户服务，也未重复全量本地测试；最近 266 项本地通过记录仍是源码验证，不构成官方发布兼容证明。此前临时安装路径不能按假定布局直接定位 dsh-session（会话包），需要重新解析实际包布局后再做运行验证；不得据此宣称临时安装整体已消失。
- 客户端拆分增量：浮层/项目投影视图、语言文案、共享样式、资源类型判定移入独立模块，入口由 2385 行降至 1668 行。对照确认浮层定义除导出声明外保持原文，不改变槽位、主题、请求或状态行为；设置卡片与远程适配的后续拆分仍未完成。
- 最新验证：`pnpm run check` 通过，28 个文件、266 项测试和 13 项包产物检查；隔离本地开发版浏览器冒烟测试 2 项通过、8 项环境门控测试跳过。验证打开浮层、项目/需求/任务图显示、关闭并进入基础设施设置，页面身份、非空、框架错误覆盖层及控制台检查通过；1680×1000 截图已人工回读。证据 `/tmp/pactflow-client-split-YiOVY6/{overlay.png,settings.png,report.json}` 位于仓库外。未测试移动视口；Browser（浏览器）插件未列出，沿用仓库 Playwright（浏览器自动化）流程。
- 客户端职责拆分继续：资源卡片、基础设施状态展示、设置合同和设置流程独立成模块，注册/远程适配入口降至 239 行。设置流程定义除导出声明外保持原文；补上类型扩展目标的显式类型导入，避免依赖旧入口的间接导入顺序。设置模块仍有 1027 行，后续状态管理简化不应与行为修复混为同一验收。
- 最新验证：`pnpm run check` 通过，28 个文件、266 项测试及包产物检查；同一隔离开发版浏览器流程再次通过，2 项通过、8 项环境跳过。页面身份、非空内容、错误覆盖层、控制台和浮层→设置交互检查通过，1680×1000 设置截图已回读；证据 `/tmp/pactflow-settings-split-g6GyCA/{overlay.png,settings.png,report.json}`，未测试移动端，未切换用户浏览器或服务。
- 浮层持续投影修复：使用 DSH 原生 useSessions（会话订阅钩子）按浮层所属会话读取项目、需求、任务图、运行及交付投影；完整实时投影优先，初始远程快照仅在实时投影未就绪时使用。会话编号保持原生标识类型，关闭后选择器不再读取该会话投影。
- 浏览器回归证明：修复前宿主新增需求成功但浮层仍显示无数据，修复后无需刷新或重新打开即显示新需求；切回旧历史会话不显示新会话需求。脚手架原先覆盖预设根，现显式登记本插件根；新实时会话通过原生创建入口建立，使用闭合中止轮次夹具使其可列出，不发送模型请求。旧 0.1 日志展示测试保留，不改写其生产者声明。
- 最新验证：`pnpm run check` 通过，28 个文件、266 项测试及包检查；浏览器 3 项通过、8 项环境跳过，页面/控制台检查及会话隔离断言通过，1680×1000 实时需求截图已回读。证据 `/tmp/pactflow-live-projection-3pp6c1/{live-overlay.png,report.json}`。资源池容量和工作区配置仍来自初始查询，实际传输取消、移动视口及真实环境验收未完成。
- Gitea 检查取消接线：浮层控制器信号传至远程适配，宿主 `verifyGitea` 声明原生可取消接口，仓库和分支保护读取使用调用方取消与 10 秒超时的组合信号。生成接口确认携带取消描述；其余加载/探针请求仍未接通真实取消，不以本项代表全部完成。
- 最新验证：真实本地 HTTP 未结束请求的取消回归先失败后通过，同时观察请求完成和服务端连接关闭；`pnpm run check` 通过，28 个文件、267 项测试与包检查。浏览器已有流程 3 项通过、8 项环境跳过；该浏览器流程未点击受控挂起的 Gitea 检查按钮，因此“关闭浮层→实际远端连接取消”的完整端到端证明仍需补齐。未访问真实 Gitea 或修改运行服务。
- Gitea 取消端到端补证：隔离浏览器点击仓库检查，分别在仓库读取和分支保护读取阶段由本地 HTTP 服务保持响应未结束；关闭浮层后，两种连接均在 2 秒断言窗口内关闭，早于宿主 10 秒超时。使用合规测试绑定与临时环境凭证引用，仅夹具元数据，不绕过实际 Remote（远程调用）或 HTTP 链路。测试结束移除临时环境值、关闭本地服务。
- 最新验证：浏览器 5 项通过、8 项环境跳过；页面和控制台检查继续通过，`git diff --check` 通过。本轮只新增测试，未重复生产构建；最近本地 267 项通过记录仍有效。此证据覆盖上述两条只读查询，不覆盖加载、模型/集群探针或真实 Gitea 部署。
- 初始查询取消接线：浮层六个初始远程查询声明并接收同一取消信号；关闭、会话或代次变更时撤销旧请求。快照读取前检查取消，历史读取完成后再次检查，已取消时不继续宿主投影折叠；工作区查询同样在异步边界检查。
- 最新验证：读取前和读取期间取消的两项回归先失败后通过，确认被取消数据不进入投影恢复；`pnpm run check` 通过：29 个文件、269 项测试及包检查；浏览器已有 5 项通过、8 项环境跳过，`git diff --check` 通过。该浏览器套件未单独挂起初始快照请求，不能替代其完整取消验收。
- 取消边界：当前 DSH `sessionQuery.readSession()` 不接受取消信号，已开始的底层读取及其内部校验不能由本插件中断；本批只取消远程请求并阻止后续宿主折叠，不修改或绕过 DSH 持久化接口。临时任务探针的取消/清理仍待实现。
- 探针清理前置修复：发现原探针清理先删 Job（任务），再按名称标签删除全部 Pod（容器组），没有控制器身份校验。三个探针入口现保存创建响应的 Job UID（任务唯一标识）；清理只选同任务名/UID/控制器归属的子资源，删除均附 UID 条件，先子资源后任务。缺失身份或子资源删除失败时不继续删任务；外来和无所有者 Pod 保留。
- 最新验证：精确归属与条件删除回归先失败后通过；补缺失任务/容器组身份及子资源删除失败断言。`pnpm run check` 通过：29 个文件、272 项测试及包产物检查，`git diff --check` 通过。使用隔离 Kubernetes API 替身，未删除真实资源；探针密钥精确清理、取消信号贯通及清理失败的持久监督仍未完成。
- 探针密钥清理修复：Harness/API（执行器/模型接口）探针记录本次 Secret（密钥资源）创建响应的 UID（唯一标识），删除带精确 UID 条件。创建冲突或未返回有效 UID 时不删除同名资源，也不继续创建 Job（任务）；结果保持失败，清理信息保留。404（资源已不存在）仍视为幂等清理。
- 最新验证：两个探针各自的已知身份、缺失身份、创建冲突六项回归先失败后通过；`pnpm run check` 通过，29 个文件、278 项测试及包检查，`git diff --check` 通过。使用隔离 API 替身，未删除真实密钥；这些运行时身份尚未形成重启可恢复的探针清理账本，取消信号及持久监督仍需继续。
- 探针取消接线：三个探针在创建前、异步创建后、读取前后与读取日志后检查取消及宿主单调时限，轮询等待可被唤醒；清理不复用已取消信号，继续精确删除已确认资源。宿主探针入口与浮层传递取消信号，移除浮层仅提前结束等待的 Promise.race（承诺竞争）包装。
- 最新验证：创建前、密钥创建后、任务创建后及读取时取消的 11 项回归先失败后通过；`pnpm run check` 通过，30 个文件、289 项测试及包检查。浏览器已有 5 项通过、8 项环境跳过，Gitea 取消回归仍通过，`git diff --check` 通过。未执行真实集群探针取消。
- 探针取消限制：已发出的 Kubernetes 创建/读取调用未接入即时网络中断，待返回后检查取消并清理，不通过竞争返回遗弃晚到资源；底层调用永久不返回时，宿主时限检查无法执行。轮询等待取消、时限边界及重启后探针清理账本仍需补证/实现，不将上述协作式取消宣称为完整生命周期闭环。
- 探针取消/时限补证：三个探针分别新增轮询等待期间取消和宿主截止点拒绝成功响应的场景。等待断言在不推进计时器的微任务窗口确认取消直接完成等待；时限使用受控单调时钟，确认晚到结果失败且精确清理仍执行，不冒充真实网络超时。
- 最新验证：专项 17 项、完整 `pnpm run test` 30 个文件/295 项全部通过；`git diff --check` 通过。本轮仅改测试，未重复构建和包检查；底层 API 永久不返回、真实集群取消与重启清理监督仍未获得验收证明。
- Kubernetes 读取取消：确认当前客户端公开中间件支持请求信号，采用追加方式保留原认证配置，并组合已有信号、用户取消与剩余探针时限。接入三个探针的任务状态读取，以及 Pod（容器组）列表和日志读取；不以提前返回替代真实连接中断。
- 最新验证：使用真实 Kubernetes 客户端对接本地协议服务，挂起读取的取消回归先失败后通过；任务状态、容器组列表和日志三阶段均观察到连接关闭，随后独立清理请求完成。`pnpm run check` 通过：31 个文件、298 项测试及包检查；`git diff --check` 通过。测试配置仅对本地无凭证 HTTP 服务允许明文连接，不改生产 TLS 策略。
- 剩余边界：创建调用暂不随用户取消即时中断，以免丢失 UID（唯一标识）后遗弃已创建资源；创建/清理调用的独立超时与持久意图、探针重启恢复账本、真实集群和浏览器到探针取消的完整证明仍未完成。
- 探针成功判定修复：找不到日志 Pod（容器组）时抛出错误，不再返回非空诊断字符串冒充输出；API（模型接口）探针空输出不再被 Job（任务）成功状态带成绿色，并明确记录缺少响应证据。原精确清理继续执行。
- 最新验证：三个探针缺失容器组、模型接口空输出四项回归先失败后通过；`pnpm run check` 通过，31 个文件、302 项测试及包检查，`git diff --check` 通过。仅隔离测试，未运行真实集群，不将结果存在性检查视为完整镜像/容器身份及协议语义证明。
- 探针日志溯源修复：日志只读取本次任务 UID（唯一标识）控制的容器组；读取后再次查询容器组，拒绝同名替换或控制器归属变化。轮询同时拒绝同名但 UID 不同的任务。日志读取与清理共用控制器归属判断，外来容器组不读取、不删除。
- 最新验证：外来容器组唯一候选/优先候选、容器组读取期间替换、同名任务替换四项回归先失败后通过；`pnpm run check` 通过，31 个文件、306 项测试及包检查，`git diff --check` 通过。此前真实客户端对本地协议服务的读取取消测试继续通过；未验证真实集群、探针完整镜像证据及重启清理账本。
- 已知探针密钥脱敏：宿主已掌握的模型密钥在执行器/API 探针日志和错误输出中先脱敏后截断，覆盖原文、JSON 转义及 Base64（编码）形式，避免截断后残留已知密钥前缀。未将这一处理宣称为任意未知秘密检测，也未读取原有 Kubernetes 密钥内容。
- 最新验证：日志回显、编码回显、接口错误和截断边界在两个探针中的 8 项回归先失败后通过；`pnpm run check` 通过：31 个文件、314 项测试及包检查，`git diff --check` 通过。全部为明确标注的合成测试值，未读取或输出真实凭证；借用密钥的容器侧日志策略及其它结果字段的敏感数据审查仍需继续。
- 真实 Gitea 测试合同迁移：原始验证命令改为先登记工作区验证配置再按编号绑定；四个评审点使用携带当前修订和授权审计的既有隔离夹具，明确不冒充人工审批界面验收。默认分支保护改为只读前置检查，缺失时失败，不再由测试自动创建规则。
- 最新验证：相关本地收口/评审授权/验证配置 24 项通过；浏览器已有 5 项通过、8 项环境跳过。真实 Gitea 套件本轮未启用，只验证模块加载与本地合同对照，不声称远端收口通过。未读取真实令牌、未修改仓库权限或执行远端合并。
- 验证配置用户入口：新增独立编辑卡片，接入现有项目面板和保存远程接口，支持编号/名称/命令/参数数组/超时、默认收口项及撤销草稿；复用共享校验，命令解释器包装无法保存，保存不执行验证命令。默认项单独修改不重写目录，保留条目修订。
- 浏览器回归：入口缺失先复现，随后验证用户登记、宿主持久化回读、禁止命令解释器、撤销未保存名称以及默认项变更保持条目修订。截图发现卡片裁切/重叠后，采用面板现有网格卡片布局修复，并保留未裁切/不重叠断言。最终浏览器 6 项通过、8 项环境跳过，1680×1000 截图已回读；证据 `/tmp/pactflow-validation-ui-c5rVS0/{validation-profiles.png,report.json}`，未验证移动端及跨卡片未保存草稿并存。
- 本轮本地验证：完整检查 31 个文件/314 项测试与包检查通过；后续纯布局修正重新构建并复跑浏览器通过，`git diff --check` 通过。未执行配置中的命令、未改用户运行服务、未提交代码；跨配置条目修改的修订行为仍需后续使用路径复核。
- 验证配置条目修订修复：完整目录保存不再无条件递增每个条目；规范化后内容未变的条目保留修订，内容变化的条目递增，过期输入修订仍拒绝。工作区整体 CAS 修订规则不变，避免编辑一项无谓使其它配置绑定失效。
- 最新验证：两条目中仅修改第一项的回归先得到错误 `[2,2]`，修复后为 `[2,1]`；相同目录重复提交和过期修订拒绝对照通过。`pnpm run check` 通过：31 个文件、315 项测试及包检查，`git diff --check` 通过。本轮未重新运行浏览器或真实环境测试。
- 未完成验收：本批未运行真实 K3s/Gitea 和官方发行版安装验收，网页测试的 8 项环境跳过不能作为完整发布证明。旧生产者日志可读，但向旧 0.1 声明直接追加 0.3 事件会被当前 DSH 拒绝；未绕过这一外部事件边界。
- 当前阶段：按开发计划 §14.5 继续修复非法事件引用、跨进程并发、恢复占用、临时错误分类、精确子资源清理和 Git 任务集合等问题，整个目标尚未完成。命令登记不等同于操作系统沙箱，不以本批证据宣称隔离了用户允许命令所执行的项目代码。

## 2026-09-06 zcode 接续（执行中）

- 交接核对：工作树 `/Users/ty/Codes/dsh-pactflow-hardening`，分支 `codex/pactflow-hardening`，HEAD `77f7108`；未跟踪与已跟踪增量全部保留。无并行写入进程，无 `index.lock`。本轮由 zcode 作为唯一源码写入方。
- 交接失败复现：`tests/k3s-worker.spec.ts` 的 durable Job binding 失败测试按手册第 5 节命令复现，1 项失败（删除序列为空）。根因与手册一致：`run()` 在 `onBound` 回调失败后以不带 Job UID 的原规格调用 `cancel()`，走了按名称删除分支，无法满足 UID 前置条件。
- 绑定失败补偿修复：`run()` 现在在回调前构造带 `jobUid` 的绑定规格，回调失败补偿使用该精确身份走 `cleanupRun()`；不进入结果等待，原始绑定错误不被清理错误掩盖。此时子资源已完成 ownerReference 绑定，UID 精确清理成立；更早的创建窗口内私有补偿路径保持按名称。
- 创建分支取证：新增 7 项隔离替身测试覆盖输入密钥/ConfigMap/Job 创建失败、补偿失败、无 UID 响应及归属绑定失败分支。取证暴露两个真实缺口：一处补偿失败会跳过其余补偿并掩蔽原始创建错误；无 UID 与归属绑定失败路径同样存在掩蔽。
- 创建补偿修复：新增 `compensateCreatedChildren`，创建失败补偿逐项尝试全部已创建子资源（404 幂等，仅当本轮创建过模型密钥才删除它）；任何补偿失败以 `compensation of created resources also failed` 附加于原始错误之后，不再跳过、不再掩蔽。无 UID 与归属绑定失败路径同样保留原始错误。
- 最新验证：上述 7 项测试按先失败后通过完成；`pnpm run check` 通过：31 个文件、322 项测试及 13 项包产物检查，`git diff --check` 通过。首轮全量曾出现 4 项真实 Git 测试 5 秒超时，单独重跑全部通过（与既有并发波动记录一致，断言与超时未改动）。
- 探针崩溃残留加固：三个探针 Job 增加 `ttlSecondsAfterFinished`（1 小时），配合既有运行时限使宿主崩溃后的探针 Job/Pod 由集群侧 TTL 控制器回收；探针模型密钥在 Job 创建返回 UID 后尽力绑定 Job ownerReference，正常路径仍以进程内 UID 前置删除为准，绑定失败不改变探针结果与清理。开发计划已补充探针临时资源合同；TTL/绑定只是兜底，重启可恢复的探针清理账本仍未实施。
- 探针证据：新增 4 项隔离替身测试（harness/api 密钥归属绑定与 TTL、绑定失败不破坏探针、image 探针 TTL）先失败后通过；`pnpm run check` 通过：31 个文件、326 项测试及 13 项包产物检查，`git diff --check` 通过。未运行真实集群，集群侧 TTL 回收行为需真实 K3s 验收证明。
- 探针清理账本增量：开发计划新增探针清理账本合同后实施。三个探针在创建任何 Kubernetes 资源前向持久账本写入意图（种类、名称、连接指纹、时间），创建响应返回 UID 后立即确认补全，进程内精确清理成功后移除记录；持久化失败时探针拒绝创建外部资源（先失败后通过证明不产生孤儿资源）。账本原子写、0600、损坏失败关闭、不存凭证；崩溃于创建确认窗口的记录保留且不按名称删除。
- 宿主对账：启动时扫描账本，按 worker 连接指纹（命名空间、kubeconfig 路径、上下文的非凭证哈希）匹配当前 Provider，以 UID 前置精确清理并 404 幂等；指纹不匹配或身份未确认的记录保留、每次启动重试；身份完整但清理失败的记录由启动监督有界退避重试。
- 账本证据：账本 5 项（含损坏拒绝、无效行跳过、跨实例恢复）、worker 里程碑/失败关闭/保留记录 4 项、`cleanupProbeIdentity` 4 项、宿主对账矩阵 5 项（含真实 worker 对未确认身份失败关闭）均按先失败后通过完成。`pnpm run check` 通过：33 个文件、343 项测试及 13 项包产物检查，`git diff --check` 通过；一轮全量曾出现 3 项真实 Git 5 秒超时，单独重跑与复跑全量均通过（断言与超时未改）。网页冒烟 6 项通过、8 项环境跳过。真实集群对账验收仍未运行。
- 目标架构基线：新增 `docs/architecture-目标架构.md`（用户确认的终局意图、十条能力、不变量、永久非目标与完成定义）；开发计划新增 §15 目标架构对照与收口计划，差距矩阵确认 A 类可实施缺口 9 项、B/C/D 类阻断不变。后续工作对照 §15 优先序推进，条目状态只记录于本文档。
- §15-A1 独立时限：K3s Worker 全部创建/替换/删除调用（运行、探针与清理路径）接入 `withRequestDeadline`——独立 30 秒截止、中止信号经中间件真实断连（与既有读取链同机制），竞速兜底保证宿主不再被挂起调用永久阻塞。超时属"创建结果不明"：运行路径按私有创建窗口合同以逐运行名称补偿（模型密钥逐运行唯一已核实）；探针路径置状态为身份未确认、清理账本保留责任，不按名称删除。7 项挂起注入回归（运行任务/模型密钥创建、cleanupRun 删除、三类探针创建与删除）先失败后通过。
- §15-A2 敏感数据审查：核实模型凭证仅经 secretKeyRef 注入容器，API 探针脚本与 Job 规格不含明文；新增 3 项回归断言三类探针与运行路径的 Job 规格 JSON 不含凭证字面量且走 Secret 引用。审查结论：探针结果字段（输出/阶段/载荷）沿用已知密钥先脱敏后截断标准，无新增缺口；不扩大为任意未知秘密检测。
- 最新验证：`pnpm run check` 通过：33 个文件、353 项测试及 13 项包产物检查，`git diff --check` 通过，无未处理错误。下一项为 §15-A3 宿主按职责拆分（大重构，独立验收）。
- §15-A3 宿主拆分（进行中）：确立"实现函数 + 显式宿主接口"拆分模式——正文逐字迁移、`this.`→`host.`、方法名保留在类上作薄委托，宿主对象经工厂逐次构建使实例级覆盖（测试 Reflect.set）保持权威，可变标志（cleanupStopped）以 getter 保持活性读取。已提取两个接缝：清理账本（`src/host/cleanup.ts`，追加/调度重试/互斥执行/交付门禁/K3s 清理/重试/行动/恢复对账，230 行）与探针对账（`src/host/probe-recovery.ts`，记录器与启动对账，52 行）；`index.ts` 由 3442 行降至 3355 行。全部 Remote 接口、类字段与导出保持不变。
- A3 阶段验证：构建通过；`pnpm run check` 33 个文件、353 项测试（与拆分前完全一致）及 13 项包产物检查通过，`git diff --check` 通过——行为基准逐项对齐。剩余接缝：调度/派发（约 400 行）、设置适配与运行时选择（约 120 行）、恢复监督与会话辅助，后续按同一模式继续提取。
- §15-A3 调度/派发接缝：提取 `src/host/dispatch.ts`（547 行）——队列准入与取消事件、K3s 派发全链（路由解析/快照重核/claim/租约续期/Job 绑定/结算/清理保障）、Git 派发、本地 Subagent 执行、`resolveK3sDispatch`/`workerForRun`。类上保留全部同名薄委托与 `@Remote` 入口，`dispatchHost()` 工厂使测试实例覆盖保持权威。`index.ts` 降至 3030 行。
- A3 调度阶段验证：构建通过；`pnpm run check` 33 个文件、353 项测试（与拆分前完全一致）及 13 项包产物检查通过，`git diff --check` 通过。A3 剩余：设置适配与运行时选择（约 120 行）、恢复监督与会话辅助（约 300 行）。
- §15-A3 设置适配接缝：提取 `src/host/settings-adapter.ts`（28 行）——`k3sFrom`/`infrastructureFrom`/`rebuildPoolWorkers` 纯函数化，requireInfrastructure 留在类上。
- §15-A3 恢复监督接缝：提取 `src/host/recovery.ts`（477 行）——会话级恢复对账、本地 Run 过期计时器、K3s 会话盘点与恢复准入屏障、恢复运行全链（身份核验/观察/租约续期/终态结算/临时错误有界退避延迟重试）、取消贯通与恢复错误分类。`recoveryStopped` 经 getter 保持活性读取；组内互调全部经宿主路由回实例方法。
- A3 收口验证：构建通过；`pnpm run check` 33 个文件、353 项测试（与拆分前基准完全一致）及 13 项包产物检查通过，`git diff --check` 通过。`index.ts` 最终 2753 行（原 3442），五个宿主模块共 1334 行：cleanup 230、dispatch 547、probe-recovery 52、recovery 477、settings-adapter 28。全部 Remote 接口、类字段、导出与测试实例覆盖语义保持不变。
- §15-A4 设置拆分（进行中）：第一批提取 `src/client/settings-model.ts`（296 行）——六类资源卡的纯模块级助手：空配置、编辑器列定义、new* 工厂、编号生成、凭证 Ref 生成、探针阶段文案映射、基础设施规范化与资源行/凭证/替换/删除操作。`settings.tsx` 降至 778 行，`PactFlowSettingsCard` 的卡片生命周期合同（单活动草稿、测试后保存、保存收缩、删除确认）与导出原样保留。
- A4 第一批验证：构建通过；`pnpm run check` 33 个文件、353 项测试及 13 项包产物检查通过；网页冒烟 6 项通过、8 项环境跳过（与基线一致）。第二批（探针执行与凭证活动闭包，经显式控制器接口）待续。
- §15-A4 第二批：提取 `src/client/settings-probe.ts`（252 行）——凭证互斥门禁（临时 Ref 创建/释放、活动开始/结束）与探针执行全链（凭证换入临时 Ref、测试调用、结果阶段文案、镜像拉取凭证与 Harbor 制品发现、Harness 模板同步保存），经 `SettingsCredentialController`/`SettingsProbeController` 显式接口路由回组件当前闭包。组件内新增 `probeController` 逐渲染构建，探针入口保持同名薄委托。`settings.tsx` 最终 646 行（原 1027）。
- A4 收口验证：构建通过；`pnpm run check` 33 个文件、353 项测试（与拆分前基准完全一致）及 13 项包产物检查通过，`git diff --check` 通过；网页冒烟 6 项通过、8 项环境跳过。设置域最终形态：settings.tsx 646 + settings-model.ts 272 + settings-probe.ts 252 + settings-contract.ts 59。卡片生命周期合同与导出不变。
- 并行写入核查：A1/A3/A4 由另一接续会话于 18:12 前完成，本写入方 18:38-18:40 只读核查确认写入已停止（26 分钟静止）、目标架构文档未被改动，并以完整检查（353 项）验证其全绿声明后接续。
- §15-A2 敏感数据审查：逐项核对三个探针结果对象与容器侧脚本。已覆盖确认：output 与错误消息经已知密钥三变体（原文/JSON 转义/Base64）先脱敏后截断，`JSON.stringify(result)` 全对象断言覆盖 stages 与全部字段；API 探针脚本凭证只从环境变量读取、仅进入请求头，HTTPError 响应体经容器日志回传后在宿主侧脱敏；requestPayload 为纯 model/prompt 结构；traceback 不回显环境变量。新增两条防漂移回归：requestPayload 不含凭证材料且结构仅含 model/messages/max_tokens；探针脚本源码（已导出 `API_PROBE_SCRIPT` 供静态审计）不打印请求头或请求对象，错误回显路径经 probeLog 脱敏。声明边界不变：已知密钥脱敏不构成任意未知秘密检测；baseUrl 内嵌凭证属用户配置问题，宿主不掌握其密钥字段；容器内第三方镜像脚本不受本仓库控制，宿主以日志读取后脱敏兜底。
- A2 验证：新增 2 项回归通过（防漂移断言，非缺陷修复）；`pnpm run check` 通过：33 个文件、355 项测试及 13 项包产物检查，`git diff --check` 通过。
- §15-A5 容量与工作区配置实时投影：浮层新增 `loadRuntime` 失效重查——以实时投影的运行状态/清理状态/需求数签名识别变化，就绪状态下签名变动时经独立 Remote 仅刷新 workerPools 与工作区配置，失败保留旧值待下次变化重试，关闭/会话/代次切换时中止并复位签名。实现与端到端测试同批完成（新能力，非缺陷修复，无先红后绿记录）。测试中暴露并修复一个真实缺陷：签名引用跨会话/重开不复位，会把上一所有者的签名带入下一轮抑制首次刷新，已在既有清理副作用中复位。
- A5 验证：新增端到端测试证明完整链路——打开浮层断言修订 R1 → 浮层打开期间 RPC 变更工作区配置（R2，浮层不感知）→ 投影移动（新增需求）触发刷新 → 浮层无重开显示 R2 与新需求。网页 7 项通过、8 项环境跳过；`pnpm run check` 通过：33 个文件、355 项测试及 13 项包产物检查，`git diff --check` 通过。移动视口仍属 A8。
- §15-A6 调度公平性证据：补两条组合调度器证据测试——跨项目交错请求按全局到达顺序准入，且项目内配额（2）耗尽时第五个请求正确排队而池有空位；被阻塞的队首（项目 A 配额满）不被后续可准入的项目 B 跨越，队首解阻后同周期按序链式排空。证据测试修正了两个错误期望后按真实合同通过（项目配额排队与队首解阻链式排空均为既有设计语义，非缺陷）。`pnpm run check` 通过：33 个文件、357 项测试及 13 项包产物检查，`git diff --check` 通过。
- §15-A7 锁补验：补"释放路径中途崩溃"双模式恢复测试——锁目录已原子改名至 released- 名但所有者文件未及删除时，死回收者的残留由下一次锁获取自动完成认领与清理，活回收者的残留保留且不阻塞新锁。既有覆盖确认：发布/暂存/已移走三态死主回收、活暂存保留、旧格式超时不抢锁、12 进程同修订竞争与死主恢复竞争均已存在。跨操作系统差异按 §15 以条件记录为准，不在 macOS 上虚构通过。
- 测试并发调整：新增测试使全量变重后，真实 Git 测试在 maxWorkers=2 下两轮稳定出现 20+ 项 5 秒超时（单独重跑均绿；单 worker 单文件亦绿）。沿"动并发、不动断言与超时"的既有先例，`test` 脚本并发降至 `--maxWorkers=1` 后全量恢复稳定：33 个文件、359 项测试及 13 项包产物检查通过，网页 7 项通过、8 项环境跳过，`git diff --check` 通过。断言与测试超时未改动。
- §15-A8 网页边界验证（部分）：新增移动视口测试（390×844，共享桌面页缩放视口以保留会话选中态）——浮层 panel 全部落在视口宽度内、关闭后无残留、项目面板验证配置编辑器无横向滚动、无框架错误遮罩与控制台错误，通过。DSH 壳自身的窄屏导航形态属 DSH 核心 UI，不在插件断言范围。跨卡片未保存草稿并存仍开放：隔离环境无已保存基础设施卡夹具，构造成本高，保持为显式缺口。网页现 8 项通过、8 项环境跳过。
- §15-A9 验证配置跨条目修订使用路径复核：新增真实宿主路径测试（隔离 DSH_HOME + 真实 saveValidationProfiles 远程入口 + assertValidationProfilesCurrent 授权核对）——修改 first 条目后已绑定 second（revision 1）的绑定保持授权；修改 second 本身后同一绑定快照被拒（changed after Git binding）。条目级 [2,1] 修订行为经由宿主保存入口传导到授权核对，闭环确认。`pnpm run check` 通过：33 个文件、360 项测试及 13 项包产物检查，`git diff --check` 通过。
- 三项目标决策已裁决（用户 2026-09-06）：1 选 B——认领仅检查节点就绪为终局合同；2 选 A——创建结果不明采用半自动对账；3 选 B——远程企业平台列入永久非目标。已写入目标架构 §5/§6 与计划 §15。
- §15-A10 创建结果不明对账（按决策 2 实施）：gitea 客户端新增只读候选列表（精确仓库 + 所有者名下过滤，凭证脱敏 clone URL）与按编号取仓库两个方法；宿主新增 `listWorkspaceRemoteCandidates`（只读候选与意图逐项比对：精确名称/默认分支/可见性/描述操作编号线索，不持久化）与 `confirmWorkspaceRemoteCandidate`（按编号复核四项精确匹配后才 CAS 将 creating→created 地址；确认后由既有受守卫创建路径接续推送完成）；非 creating 状态两入口均拒绝；provider 快照解析失败关闭。项目面板在结果不明状态渲染对账卡片（查询候选→展示比对→输入精确编号→确认后引导既有推送）。
- A10 证据：6 项测试（只读性、缺失/名称/可见性/分支四类不匹配拒绝且零写入、确认后真实临时 Git 接续推送完成、重复确认拒绝）先红后绿；`pnpm run check` 通过：33 个文件、366 项测试及 13 项包产物检查，网页 8 项通过、8 项环境跳过，`git diff --check` 通过。
- 测试超时适应：机器负载上升后真实 Git 用例在默认 5 秒下于单 worker 全量中稳定越界（单独重跑均绿）。沿"断言不动"边界，对 7 个真实 Git 重型文件显式 `vi.setConfig({ testTimeout: 20_000 })`（文件级，仅环境容忍度），全量恢复稳定。
- 待授权事项：本轮改动未提交；提交、合并、推送与主目录同步需确认精确范围后由用户授权。真实 K3s/Gitea、官方发行版安装验收仍未运行，不得宣称通过。
- 未完成边界：容量与工作区配置实时投影已完成（见 A5）；探针创建/清理独立时限与重启持久清理账本已完成（见 A1 及探针账本增量）。仍开放：全部 Settings/Remote/投影模式统一、跨平台锁及全部崩溃窗口、调度公平性证据、网页移动视口与跨卡片草稿、两项待用户决策（节点准入、Gitea 创建结果对账）对应的被阻塞工作，以及全部真实环境与官方发行版验收。

## 2026-09-04 历史记录（完成声明已被上述复核纠正）

- 当前分支：`codex/pactflow-hardening`（独立 Worktree `/Users/ty/Codes/dsh-pactflow-hardening`）；主目录 `main` 未修改。
- 当前里程碑：阶段一至三源码实现已完成并提交；DSH 核心、Hermes 与旧 `data-governance` 工作区未改动。
- 已落地：DSH Approval（审批）授权评审与旧事件只读门禁、Workspace Validation Profile（验证配置）/CAS（原子修订写入）、K3s nonce/spec/claim/Job UID 绑定与 Job-owned Secret（任务所有短命密钥）、统一双容量 FIFO（先进先出）准入、取消/重启恢复、cleanup ledger（清理账本）与显式重试、Git tip/任务集合校验、事件 fold schema（折叠模式）校验、Overlay generation/Abort（代际/中止）保护、Harbor/Gitea 分页和反向代理路径、语义探针、Web E2E scaffold 修复与 `check:release`。
- 最后已验证提交：当前分支 `HEAD`（`加固 PactFlow 安全边界与运行可靠性`）。最近验证：`pnpm run check:release` 通过（`check`、14 个测试文件/71 项测试、包产物、Web suite 2 项测试、Profile 安装/启动/重复安装/卸载/清洁启动）。
- 当前 blocker：尚未执行真实 K3s/Git 清理矩阵和主目录收口；不发布 npm、不删除历史 34 个无所有者 Pod，除非另行授权。
- 下一动作：若要进行真实 K3s/Git 清理矩阵或同步 `main`，需在具备对应凭证和运行环境后单独授权；当前分支可作为恢复 carrier。

- 当前里程碑：6 增量 —— 资源化基础设施与逻辑 Worker Pool
- 状态：`LOCAL_UAT_PASSED（本地用户验收通过）`；代码、自动回归、双远端推送和 9120 冷恢复验收已通过，未打包、未发布
- 最后已验证提交：DSH 通用能力分支 `aa888ad0fb`；Bundle 功能提交 `28defce0967b19a5fbac965d97817b78fc0c5ca8`

## 已完成合同

- 外部 Bundle：单一 Loader 身份交付 Host、Agent Preset、Client、Typert 和 Remote；临时 Profile 的 tarball 安装、配置展开、启动、卸载和无插件重启已通过。
- 持久事实：0.2.1 写入 13 类带 `v: 1` 的外部 Session Event，并以 read-only 注册兼容 0.1.0 的 12 类词汇与 0.2.0 的 13 类词汇；5 个 Projection、十阶段与四个人工门禁、DAG revision CAS、Run claim/renew/settle、租约边界和第一终态获胜已通过回归测试。
- 当前模式权威：`SessionHeader.agentPreset` 只表示创建时模式；项目发现、授权和冷恢复以 `agentPreset` Projection 的当前值为准，裸 Host 嵌入才回退到 Header。会话 Need/DAG/Run 仍由 Session Projection 持有；工作区级 Git 与 Agent 策略由插件自己的 `workspace-projects.json` 按稳定 `workspaceId` 持有，不进入 Settings。
- 只读编排器：零脉 Preset 在每个真实 Agent Scope 上限制继承工具，只暴露 `read/read_image/glob/grep` 与受控 PactFlow 工具；`bash/pwsh/write/edit` 和模型侧 `pactflow_dispatch_local` 不进入编排器请求头，强行构造调用也由同 Agent 的执行前门禁返回中文引导。所有代码施工必须通过 `pactflow_dispatch_git` 或 `pactflow_dispatch_k3s` 进入隔离任务分支；普通 DSH 会话与 Worker 子作用域自行注册的施工工具不受影响。
- 本地 Run 恢复：Host 继续只读兼容历史 `dispatchLocalNode()` 事件与冷恢复，但当前零脉 Preset 不再向模型暴露同工作区本地派发工具。Host 冷重启后不伪造重连只存在旧进程内存中的 `spawn/fork` Worker；非终态历史本地 Run 在租约内保留所有权，到期后幂等追加过期失败并将同 revision 节点恢复 `ready`。`pactflow_retry_node` 只允许无活动 Run 的 `failed/cancelled` 节点通过 revision CAS 恢复，旧 Run/claim/outcome/attempt 不覆盖，新派发创建 attempt+1。
- Agent 评审门：`pactflow_record_review` 将 requirement/design/plan/code/verification 的 approved/rejected/changes-requested 决策写入当前 Need Session Event，并与 `pactflow_transition_need` 组成可持久、可审计的人工门禁链；不再存在 Host Remote 可用但 Agent 无法记录决策的断路。
- Git worktree：DSH 通用 `SubagentStartRequest.cwd` capability 已在独立上游分支完成；零脉可绑定无密钥远端身份，从远端跟踪分支冻结 base commit，由 Host 生成任务分支与 DSH Home 下的 worktree，并只接受该分支上干净、可追溯的后代 commit。
- Git 同步与验证：项目可保存 HTTPS 用户名和 DSH Credential Ref，不保存 token；Host 在清除凭证环境后执行结构化 `{command,args,timeoutMs}` 验证，复核 commit/clean tree，再用私有临时 AskPass push 任务分支并由根 checkout fetch/比对 commit。真实 Basic Auth Smart HTTP 集成测试通过，验证失败不会 push。
- K3s Provider：使用 Kubernetes Client、不可变 ConfigMap/Job、digest 镜像、无 ServiceAccount token、非 root UID/GID 1001、SecretKeyRef、严格 SSH known_hosts 和 0 backoff；Pod 只 push 任务分支并通过 termination document 报告，Host fetch/快进本地 worktree、执行验证后才结算。Agent 恢复时会按持久 Job/spec 对账，接受租约内已完成结果、继续等待活动 Job、取消过期 Job或失败关闭缺失 Job。
- K3s 终态清理：ConfigMap 与短命 Model Secret 带 Job ownerReference；清理改为先删子资源、再删 Job，两层 404 均视为幂等已清洁。禁止三者并发删除与 Kubernetes GC 竞态产生 409，Closing 不再把已完成 Job 误报为 cleanup failure。
- Harness/协议矩阵：Harbor 实际项目为 `datavdl`，治理后镜像精确为 Claude Code、Codex、OpenCode、DSH 四个 digest；协议分别固定为 Anthropic Messages、OpenAI Responses、OpenAI Chat Completions、OpenAI Chat Completions。`probeHarness` Remote 与原生模板表会显示 template/image/model/baseUrl/API mode/真实 prompt、逐步状态和有界输出；四个真实 K3s Harness 探针均返回 `hi` 并自动清理 Job。
- Settings 与测试 UI：`pactflow` Settings namespace 持久化非密钥 K3s/Harness 元数据并声明 restart-applied；原生 Plugins 配置卡可查看模板、透明编辑/禁用并持久保存。控制台模板表提供 API/Harness 双测试，默认真实内容 `say hi to me`，显示 path/payload、阶段和滚动输出；四种协议的直接 API 探针与四个 Harness 探针均通过。
- Gitea admission：Git 绑定保存 credential-free API base/owner/repo/token Credential Ref；Host 只读核验仓库 full name、默认分支、归档状态、branch protection、required approvals、status checks 和 merge style，Remote/UI 不返回 token。专用 `PACTFLOW_GITEA_API_TOKEN` 以最小 `write:repository + write:user` scope 保存于权限受控 DSH Credentials。
- Gitea 认证兼容：Git/Gitea 绑定显式支持 `giteaUsername`，使 Password Credential 走 Basic Auth；历史 Session binding 未持久 username 时，Host 会按 credential-free base URL + Credential Ref 从当前 Provider 补回非密钥 username。Closing Basic Auth 回归覆盖已验证，不再把密码误作 API token 发送。
- Closing：只有 Need 位于 closing、最新 verification review 批准、全部 DAG 节点成功、Gitea 默认分支受保护且没有未满足 approvals/status checks 时，Host 才在隔离 worktree 合并任务 refs、复验、push integration branch、创建并合并 PR、核验默认分支 ancestry、记录 release event 并推进 deployed。真实 Gitea 1.22 验收发现 PR 创建后的异步 405 窗口，Client 现等待精确 head 可合并并只重试 transient 状态；永久错误失败关闭。完成后精确删除 integration/task worktree、本地/远端任务分支及 K3s Job/ConfigMap。
- 原生 Web：外部 Client Module 的条件 Header Action 与 `shell.overlay` 读取真实 Projection/Remote；冷 Session 可恢复并显示 Project、Need、DAG 和 Run，不存在 iframe、第二 Web 壳或 mock 数据。
- 资源化基础设施：Settings 新增 K3s Cluster、Harbor Registry、Gitea Git Provider、Harness Template、Model Connection 和 Worker Pool 六类引用资源，保留旧 `k3s` 读取兼容但禁止两套调度源同时启用。
- 逻辑容量池：每个 Pool 绑定 Cluster/Registry/Templates，以 FIFO 管理 `maxConcurrency`；排队、等待取消、幂等释放、重启时对活动 Job 恢复容量占用均有代码边界。
- 自动 Gitea 匹配：Git 绑定从本地 remote 读取无凭证 URL，按 host 匹配全局 Provider 并提取 owner/repo；多匹配失败关闭。
- 连接测试：Cluster 读取真实 Namespace；Harbor 调用 v2 ping/project API、只访问配置的 `pactflow-worker` 仓库并识别四类 Harness 镜像；Gitea 调用 version API；Harness 已保存卡片在 K3s 中拉取镜像、运行独立 CLI `--version` 临时 Pod，不再错误依赖 Worker Pool 或模型；Model 发送真实消息；Pool 验证资源图。Remote 只返回分阶段脱敏证据。
- 测试凭证事务：未保存的 Harbor/Gitea/模型凭证只写入随机 `_PROBE_` 临时 Credential Ref，测试草稿引用临时 Ref；测试、模型发现、清理与保存共享互斥门禁，保存时先确认临时 Ref 清理成功，再写正式 Ref。取消或保存时清理失败会保留引用以便重试并给出明确反馈；K3s 临时 Job、Pod 或模型 Secret 清理失败会使整体测试失败，不再显示绿色成功。真实浏览器验收确认模型发现期间临时 Ref 数量从 0→1，取消后回到 0，正式 Ref 未被覆盖。
- 设置 UI：六类基础设施资源均为 DSH 原生风格的多行响应式卡片；一次只编辑一张卡片，成功测试且表单未变化时才允许保存。保存后收缩为摘要，支持编辑回显、取消、删除影响检查和二次确认；高级 JSON 仅用于只读排障，容量快照在控制台显示运行/等待数。
- 易用性收口：Kubeconfig 使用 DSH Host 原生文件选择并从文件解析 Context 下拉；Harbor/Gitea/模型页面显示账号和写后不可读的密码框，内部 Credential Ref 隐藏；Harbor 连接成功后从 `pactflow-worker` 自动同步 Claude Code、Codex、OpenCode、DSH 四个模板的不可变 digest，保留人工资源规格；K3s imagePullSecret 由真实服务列表选择。
- 测试可观测：K3s、Harbor、Gitea 和执行资源池的测试直接使用当前未保存卡片草稿；每张卡片就地显示准备、凭证、配置校验、连接、资源发现和详细失败日志，不再把错误写到设置卡底部或浏览器控制台。
- Harness/模型解耦：Harness Profile 只保存工具、Harbor Artifact 和资源规格，Model Connection 独立保存协议、model ID、base URL 和 DSH Credential；Host 在 Run 时组合兼容项并创建 Job-owned 短命 Kubernetes Model Secret。
- 模型发现：模型连接按“协议 → URL/API Key → 加载模型 → 下拉选择 → 真实测试 → 保存”配置，复用 DSH `llm-pi-ai` discovery；结果按 model ID 去重并显示 `name (id)`，当前 ID 未被服务端返回时明确标为未验证。发现失败保留详细错误和显式手工填写入口。
- 可用性状态：六类已保存卡片均显示可用性测试按钮和灰/黄/绿/红状态灯；Host 将最近结果、时间、配置指纹与脱敏分步日志保存到插件独立运行状态，重进页面和重启后恢复，配置变化自动失效，超过 5 分钟标记过期，不做后台轮询。
- Worker 并发与调度：配置向用户只展示名称、运行集群、允许调度的 Harness、同时运行的 Worker 上限和 Harbor 镜像拉取密钥；内部 ID、Registry 引用和固定 FIFO 不显示。Harness 使用可点击选项，缺少兼容模型的项明确禁用；所选 Namespace 的 dockerconfigjson Secret 自动加载，唯一值自动选择。
- 严格插件化项目入口：Client 只注册 DSH 现有 `sidebar.footer.action`，左侧栏底部显示“零脉项目”；不修改工作区菜单、不注入 DOM、不覆盖 DSH 全局 CSS。面板从 Workspace API 读取工作区并通过 Typert Remote 管理插件自有项目数据。
- 工作区 Git 向导：可只执行安全的本地 `git init`（不暂存/提交文件）、识别现有 origin、迁移会话 Git 配置，并在显式二次确认后使用全局 Gitea 创建仓库；AskPass 凭证只进入子进程环境，不进入 argv、URL、日志或项目文件。
- Agent Profile：项目先指定一个 K3s 集群和该集群下的执行资源池，再动态组合多个稳定身份的 `Harness + 兼容模型 + Worker 数量` 规格；同一 Harness 可搭配多个不同模型，重复组合失败关闭。项目可用 Worker 总量由各规格数量求和得出，不再设置第二个手工项目总并发；实际运行仍由全局 Pool FIFO 限流，Run 固化 `projectConfigRevision` 与 `agentProfileId`。

## 最新真实验收

- `pnpm run test:real-worker`：真实浏览器选择零脉模式，真实 DeepSeek 父 Agent 调用初始化、Git 绑定、Need/Node 创建与 Git 派发工具；DSH `spawn` 子 Agent 在独立 worktree 创建文件、验证、`git add/commit`，Host 校验 branch、clean tree、base ancestry 和 commit 后记录成功 `pactflow/run-settled`。
- `pnpm run test:real-k3s`：真实 DSH K3s Pod 基于 `tianyue/zeromai-demo` base commit 创建证明文件并提交/push 随机任务分支，Host fetch 到对应本地 worktree、运行 `/bin/test` 并成功结算；测试 Job、ConfigMap 和远端分支已全部删除。
- `pnpm run test:real-gitea`：在受保护的 `tianyue/pactflow-acceptance/main` 上，由零脉 Host 创建任务与 integration 分支、真实 PR、等待异步 mergeability、合并、更新 main、记录 release/deployed，并清理全部临时分支和 worktree。
- npm 包名安装：0.2.0 暴露了 Host Typert 404；0.2.1 的 Profile 门禁新增真实 cookie exchange 与 `/api/pactflow/health` Remote 验证，覆盖 Package 下载、Bundle/Preset 激活、真实 Web 启动、卸载后 404 与清洁启动。
- 凭证只由测试启动器从 DSH Credentials 读取并作为子进程环境传入；原值未进入 argv、URL、Session Log、Tool result、测试输出或 Git。
- 调试修复了两个真实边界：活动模式必须读 Projection，而非不可变 Header；Preset 内部 Package 必须包含 name/version，才能通过 DeepSeek request extension inventory 校验。
- 真实业务 DAG 与受保护 main 收口：`data-governance` 的 `quality-issue-closedloop-gaps` 七个节点（五功能 + 跨节点 code-review fix + Closing 集成祖先）均通过工作区 `dsh-model` Agent Profile 在真实 K3s Job/Pod 中修改代码/集成分支、commit/push Gitea，并由 Host 隔离 worktree 强制 Maven 验证后结算 `succeeded`。N6 修复了 resume 归属死路、聚合 SLA 双延期、null-safe 精确去重与新重跑结果 ID 关联；N7 保证 6 个已验证 commit 全为祖先且最终 tree 同 N6。五条 review event 均 approved，Gitea PR #1 已合入受保护 `main`=`7676ee267aef0dd3d46d3e28085b74a3428cc868`，Need=`deployed` revision 10，release 已登记，任务/closing 分支与 K3s Job/Pod 已清理为 0。

## 当前边界与下一安全动作

- 完整任务矩阵：Claude Code、Codex、OpenCode、DSH 均已在真实 K3s Pod 中修改 `zeromai-demo`、测试、commit/push，Host fetch 到独立 worktree 并运行 `git diff --check` 后成功结算；三条新增矩阵与既有 DSH case 均已删除 Job、ConfigMap 和远端测试分支。
- 离线恢复：`dist/` 包含 0.2.1 npm tarball、插件完整 Git Bundle、DSH 三提交通用前置分支完整 Git Bundle 和 `SHA256SUMS`；三项校验与两份 Bundle 完整历史验证均通过。
- npm 分发：Package 采用 Apache-2.0，所有未发布的 DSH/Cordis/React in-box peer 保留版本声明并标为 optional，由 DSH 安装本身解析；用户通过官方 `dsh plugin --profile web add dsh-pactflow@0.2.1` 安装。
- 公共发现：源码同步到 `https://github.com/tianyuegithub/dsh-pactflow`，带 `dsh-plugin`、`deepseek-harness`、`pactflow`、`multi-agent` topics；GitHub Release tarball 与 npm 包名都走 DSH 官方安装命令。
- npm 发布：0.2.0 已 deprecate；0.2.1 修复外部 Typert package identity 并保持历史事件兼容。7 天 granular publish token 按产品 Owner 要求暂时保留，便于测试期修复重发。
- 当轮验证：`pnpm run check` 通过，包含构建、13 个测试文件/65 项测试和 13 项发布产物检查；新增覆盖本地 Run 过期回收幂等性、Agent 恢复后定时回收、活动 Run 重试拒绝、revision CAS/attempt 递增、Agent 评审工具、Gitea Basic Auth 历史 binding 兼容、K3s child-first 终态清理、临时凭证清理失败重试与并发替换失败关闭。Gitea main 干净 clone 的 `mvn -o test -pl dg-quality -am` 为 103/103 通过；`dg-ui-hdmy` Vite production build 通过；`dg-ui` 在 Node 22 下以 `NODE_OPTIONS=--openssl-legacy-provider npm run build:prod` 通过。`git diff --check` 通过。全仓 lint 仍有 5.7 万个历史基线问题，不归因本变更。
- 浏览器生命周期验收：9120 本地 UAT Profile 已真实完成 K3s 卡片“新增 → 测试日志成功 → 保存解锁 → 摘要收缩 → 编辑原值回显 → 修改后取消不污染 → 关闭再进入仍持久化 → 删除影响提示与二次确认 → 删除后重载为空”。测试前保存禁用、修改已测试字段后测试失效也已验证；磁盘 `settings.yaml` 最终恢复为 `clusters: []`。
- Harbor 回归验收：真实 `datavdl/pactflow-worker` 通过完整测试并显示绿色联通状态，自动持久同步四个 Harness 模板；不再读取 Prometheus 等其它项目仓库。既有 `datavdl/` 配置自动规范化为 `datavdl`。关闭设置和重启 Host 后，绿色状态与模板仍恢复。
- 模型发现验收：真实 Coding API 使用已保存 Credential 加载并去重为 130 个服务端模型，加上占位项和当前未返回的 `ark-code-latest` 共 132 个唯一下拉值；Anthropic Messages 不支持列表时显示原始兼容错误并出现“手动填写模型 ID”入口，取消编辑不污染保存配置。
- Harness 状态回归：旧列表测试因没有 Worker Pool 误报红色；新合同将 Harness 镜像测试与模型 API 解耦，并使旧合同记录自动失效。已加入执行资源池的 Harness 会按每条资源池路由分别使用对应 K3s 集群和镜像拉取密钥测试；未加入资源池时才对全部配置集群执行独立镜像测试。Harness 模板与执行资源池必须引用同一 Registry，禁止跨仓库混用镜像拉取密钥。Claude Code 真实创建临时 K3s Job、运行 `claude --version`、5.3 秒成功转绿且 Job 清理后集群无残留。
- Worker 调度验收：默认草稿自动选择 K3s 集群、三个具有兼容模型的 Harness 和唯一 Harbor 拉取 Secret；Claude Code 因缺少 Anthropic 模型明确禁用。资源图测试成功，草稿随后取消，未写入用户配置。
- 工作区项目与视觉验收：9120 本地 UAT 通过现有侧栏 Slot 打开项目面板，真实列出 `data-governance`、Git/工作树状态和会话迁移候选。选定视觉方案实现为模块化 Agent 组合器，在 1440×1024 下生成 DSH/Codex/OpenCode 三个规格，无横向溢出、无浏览器 warning/error；重复 Harness/模型组合被拒绝，编辑回填成功。设计对比见根目录 `design-qa.md`，最终结果 `passed`。
- Agent 数量回归：删除可编辑的“项目总并发”，项目可用 Worker 总量改为各 Agent Profile 数量自动求和；旧版持久字段仅保留模式兼容，不再形成额外调度闸门。超过全局执行资源池的瞬时任务由 Pool 统一排队。
- 只读编排器验收：真实新建零脉会话的请求头仅含 15 个只读/受控工具，明确不含 `bash/pwsh/write/edit/pactflow_dispatch_local`；首轮按指令实际调用 `pactflow_view`，Project/Need/DAG/Run 保持零状态且未产生文件修改。真实 Preset 挂载、普通 Agent 隔离和 Worker 子作用域工具恢复均有自动测试覆盖。
- 保存反馈：插件内全部保存按钮统一提供“保存中…”、DSH 原生 Toast 成功提示与包含具体原因的失败提示；长页面底部的持久状态仍保留。真实浏览器已验证 Agent 策略保存与 K3s 资源卡保存两条入口，均在当前视口顶部显示成功消息。
- 侧栏入口对齐：“零脉项目”与 DSH 原生“设置”统一整行宽度、42px 高度、图标起点、内边距、字号、行高和圆角；收起侧栏时两者均为 36×36 圆形按钮。真实浏览器盒模型测量与展开/收起截图均通过。
- 项目配置实际激活：用户批准后，将 `data-governance` 的会话 Git 配置迁移到工作区修订 1，保存三个 Agent Profile 到修订 2，并自动发现/保存 `pactflow-git-data-governance` 到修订 3。存储文件权限为 `0600`，不含密码、Token 或 API Key；页面重载和 Host 进程重启后均恢复修订 3、三个规格和 Git Secret。会话“打开零脉”读取到同一工作区修订 3。
- 2026-09-04 收口验收：`main` 功能提交 `28defce0967b19a5fbac965d97817b78fc0c5ca8` 已同步 GitHub 与内部 Gitea；9120 UAT 从该提交重新启动后恢复零脉入口、`data-governance`、Agent 策略和 11 张基础设施状态卡。真实 Codex Harness 可用性测试期间 11 个测试动作被互斥门禁禁用，约 4.4 秒后恢复并显示联通；匹配探针 Job、近 10 分钟 Pod 和模型 Secret 均为 0，`_PROBE_` Credential Ref 计数为 0，浏览器无新异常。
- 尚未完成：Gitea 新仓库创建向导未在额外仓库执行（当前 `data-governance` 已有真实远程，无必要制造第二个仓库）；DSH 三项通用前置能力进入官方发行版仍是公开稳定发布前置。本地 `data-governance` 旧工作区保留 154 个修改/171 个未跟踪的用户既有工作，不与新 Gitea main 强行对齐。
- 相关但不在当前范围：`pactflow` Namespace 仍有 34 个创建于 2026-08-29 至 2026-08-31、无 Job owner 的历史 `dsh-pf-api/image/probe` Pod；本轮未新增，不作为当前提交 blocker，未经单独授权不删除。
- 下一安全动作：保持 9120 UAT 观察；公开稳定发布继续等待 DSH 三项通用前置能力进入受支持发行版。不发布 npm 新版本，不改写用户旧 `data-governance` 工作区。
